PGHOST=localhost
PGPORT=5432
PGDATABASE=foodbudget
PGUSER=fbapp
PGPASSWORD=

# ⚠️ account와 **동일한** 시크릿이어야 access JWT 서명을 검증할 수 있다.
#    프로덕션에선 강한 랜덤값(≥32바이트) — 예: `openssl rand -base64 48`
JWT_SECRET=dev-insecure-change-me
JWT_ALG=HS256

# 공통 구조화 로그
ENVIRONMENT=development
LOG_LEVEL=INFO
