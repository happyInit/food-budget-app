PGHOST=192.168.0.8
PGPORT=5432
PGDATABASE=foodbudget
PGUSER=fbapp
PGPASSWORD=

# ⚠️ account 와 **동일 값**을 주입 — pantry 는 이 키로 account 가 발급한 access 토큰을 검증한다(A07).
#    프로덕션은 강한 랜덤값(≥32바이트): `openssl rand -base64 48`. 코드/여기 기본값은 개발용 placeholder.
JWT_SECRET=dev-insecure-change-me
JWT_ALG=HS256
