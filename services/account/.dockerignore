# 빌드 컨텍스트 = services/account/ → 여기 .dockerignore 가 적용된다.
# Dockerfile 은 requirements.txt + app/ 만 COPY 하지만, 실수로 .env(라이브 시크릿)가
# 이미지에 들어가는 것을 이중으로 차단한다.
.env
.env.*
!.env.example
__pycache__/
*.pyc
.pytest_cache/
tests/
