
# draw.io 임시/백업
.$*
*.bkp
*.dtmp

# 앱/파이프라인 비밀 — 절대 커밋 금지 (.env.example만 커밋)
.env
.env.*
!.env.example
# 예외: 운영 프론트 빌드용 공개 client_id(브라우저 노출 공개값·비밀 아님) — Vite 빌드타임 주입
!frontend/.env.production

# AWS 자격증명 CSV — 절대 커밋 금지
*accessKeys*.csv
geonu_accessKeys.csv

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
# pytest-cov 산출물 — CI 가 워크스페이스에 남긴다. 커밋되면 Sonar 가 이걸 **소스로**
#   인덱싱한다(실제로 mp-account-service ncloc 의 37%가 coverage.xml 이었다).
.coverage
coverage.xml

# 대용량 렌더 번들 (로컬/LFS만, git 제외)
**/밀플래닝 확정본.html
docs/design/food-budget 공유용.html
*Zone.Identifier
docs/db-connection-guide.md
.pgpass

# 공공데이터 대용량 원본 다운로드 (data.go.kr 15100064 재다운로드 — 커밋 안 함)
/전국통합식품영양성분정보*.csv

# 발표 시연 런북 (로컬 전용)
infra/DEMO_RUNBOOK.txt

# ── 클라우드 자격증명 (2026-07-29) ──────────────────────────────────────────
# GCP 서비스 계정 키가 커밋되면 대형 사고다 — 키 하나로 프로젝트 API 를 호출할 수 있다.
# 정상 위치는 레포 **밖**(~/.gcp/)이며, 아래는 실수로 레포에 떨궜을 때의 방어선이다.
*-service-account*.json
*serviceaccount*.json
*credentials*.json
gcp-*.json
mealplanning-*.json
application_default_credentials.json

# Claude Code 세션 산출물 — 특히 .claude/worktrees/ 는 **중첩 git 저장소**라
# `git add -A` 시 gitlink(mode 160000)로 딸려 들어가고, 그 커밋을 CI 가 체크아웃하다 깨진다
# (2026-07-30 실발생 → force-push 로 정정하다 Jenkins 리비전까지 스테일).
.claude/
.build/
