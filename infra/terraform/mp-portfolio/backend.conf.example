# backend.conf 로 복사해서 쓴다 (backend.conf 는 .gitignore).
bucket       = "mp-backup-ap2"
key          = "tfstate/portfolio.tfstate"
region       = "ap-northeast-2"
profile      = "mp-platform"
use_lockfile = true
