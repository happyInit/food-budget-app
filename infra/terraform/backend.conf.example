# 이 파일을 backend.conf 로 복사 후 비밀번호 채우기 (backend.conf 는 .gitignore).
# 사용:  terraform init -backend-config=backend.conf
# 비번은 infra/ansible/secrets.yml 의 tfstate_db_password 와 동일.
conn_str = "postgres://terraform:CHANGE_ME@192.168.0.8:5432/terraform_state?sslmode=disable"
