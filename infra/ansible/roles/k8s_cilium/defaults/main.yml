---
# Cilium 1.19.6 = 현 최신 릴리스. e2e 검증 K8s = 1.31–1.34 → 우리 1.34 가 그 상한이다
# (1.35·1.36 은 미검증 — group_vars/k8s_nodes.yml 의 버전 근거 참조).
cilium_version: "1.19.6"

# Helm 4 대신 3.x 를 쓴다 — ArgoCD 가 번들하는 렌더러가 3.x 계열이라
# 나중에 같은 차트를 ArgoCD 가 렌더할 때 결과가 갈리지 않게 맞춘다.
helm_version: "3.21.3"
