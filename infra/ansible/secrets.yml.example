---
# 이 파일을 secrets.yml 로 복사 후 값 채우기 (secrets.yml 은 .gitignore 됨).
# Harbor admin 비번은 8자+ 대/소문자+숫자 필수.
harbor_admin_password: "CHANGE_ME_Aa1!"
harbor_db_password: "CHANGE_ME_dbAa1!"
# SonarQube 내장 PG 비번 (sonarqube 롤 — POSTGRES_PASSWORD·SONAR_JDBC_PASSWORD 공용).
#   🔴 누락돼 있던 키다(2026-07-31 추가) — 이게 없으면 `--tags sonarqube` 가
#      "'sonarqube_db_password' is undefined" 로 죽는다. 호스트 C 는 이미 가동 중이라
#      **새 값을 넣으면 안 된다**: 실사용 값 = 호스트 C `/opt/sonarqube/docker-compose.yml`
#      의 POSTGRES_PASSWORD (root 전용 0640). 롤을 다시 돌릴 사람은 그 값을 그대로 옮긴다.
sonarqube_db_password: "CHANGE_ME_sonarAa1!"
#
# ── 🔴 slack_webhook_url — **부활** (2026-07-31, 같은 날 제거했다가 되살림) ──────────
#   왜 지웠었나: 유일한 참조가 은퇴한 `monitoring` 롤(alertmanager.yml.j2)뿐이라 고아로 봤다.
#   왜 되살아났나: 같은 날 만든 `hostwatch_sink` 롤(호스트 C = 클러스터 밖 관측자)이 쓴다.
#     물리 하이퍼바이저 A·B 의 비콘이 끊겼을 때 알릴 주체가 필요한데,
#     🔴 **인클러스터 정본(`fb-secrets` ns Secret `alertmanager-slack` → ESO)은 쓸 수 없다** —
#        호스트 C 는 클러스터 **밖**이고, 애초에 이 설계의 전제가 "클러스터가 통째로 죽어도
#        알림이 나가야 한다"이기 때문이다. 클러스터 안의 비밀에 의존하면 그 전제가 무너진다.
#     같은 웹훅(=`#monitoring` 채널)을 재사용해도 되고 별도 채널을 파도 된다.
#     값 확인 = 인클러스터 `kubectl -n fb-secrets get secret alertmanager-slack -o jsonpath=…`
#   ⚠️ 비워 두면 `hostwatch_sink` 롤이 **배포 시점에 assert 로 실패한다**(구 monitoring 롤의
#      "비면 조용히 null receiver" 동작을 의도적으로 뒤집었다 — 급사 알림이 조용히 꺼지면
#      존재 이유가 사라진다). 호스트 C 에는 0600 root 파일로만 떨어진다.
slack_webhook_url: "https://hooks.slack.com/services/CHANGE_ME"
#
# ── 2026-07-31 (P4) 제거된 키 — 참조 롤이 은퇴해 고아가 됐다 ──────────────────
#   tfstate_db_password · app_db_password · pgsync_db_password
#     → `tfstate_db`·`data_tier` 롤 전용이었다. DB 자격증명은 이제 CNPG 가 생성·회전하고
#       앱은 ESO 로 받는다. terraform_state DB 는 backend 가 S3 로 가면서 소멸.
#   github_runner_pat
#     → `github_runner` 롤의 compose.yml.j2(ACCESS_TOKEN — 러너 자동등록용) 전용이었다.
#       러너는 2026-07-27 Jenkins 이관과 함께 은퇴했고 롤은 2026-07-31 에 삭제됐다.
#       CI 자격증명 정본 = Jenkins 크리덴셜 스토어(`JENKINS_HOME` → `s3://mp-jenkins-backup-ap2`).
#   🔴 로컬 `secrets.yml` 에서는 지우지 않아도 된다 — 미사용 변수는 무해하고, 값 자체가
#      과거 스냅샷 복구에 쓰일 수 있다.
#      ⚠️ **단 `github_runner_pat` 은 예외** — 이건 비번이 아니라 **살아 있는 GitHub 자격증명**이다.
#         파일에서 지우는 것과 토큰을 무효화하는 것은 다르다. 러너 등록이 끝난 시점부터 이미
#         불필요했으므로(러너는 자체 자격증명 사용 — `docs/docker-infra-status.md`), 러너가
#         사라진 지금은 **GitHub 에서 revoke** 하는 것이 남은 일이다.
# ───────────────────────────────────────────────────────────────────────────
# MinIO 루트 비밀번호 (K8s) — 8자 이상
minio_root_password: "CHANGE_ME_minioAa1!"
# in-cluster Grafana 관리자 비번 — 8자 이상
grafana_admin_password: "CHANGE_ME_grafAa1!"
# ArgoCD → config 레포 읽기 전용 배포키(ssh-keygen -t ed25519 로 생성 후 GitHub 배포키 등록)
argocd_repo_ssh_key: |
  -----BEGIN OPENSSH PRIVATE KEY-----
  CHANGE_ME
  -----END OPENSSH PRIVATE KEY-----
# Cloudflare Tunnel (cloudflared 롤, 호스트 C CI webhook) — 터널 자격증명(TunnelSecret 포함).
#   값 = 호스트 C 의 ~/.cloudflared/<UUID>.json 파일 내용 그대로(한 줄 JSON).
#   tunnel_id(비밀 아님)는 group_vars/ci.yml 의 cloudflared_tunnel_id.
cloudflared_tunnel_credentials: 'CHANGE_ME_{"AccountTag":"...","TunnelSecret":"...","TunnelID":"..."}'
# ArgoCD GitHub 웹훅(즉시 CD, k8s_argocd 롤) — 기본 비활성. 켜는 법 = roles/k8s_argocd/defaults/main.yml.
#   github_secret = GitHub 웹훅 서명 검증용 공유 시크릿(openssl rand -hex 20 로 생성).
argocd_webhook_github_secret: "CHANGE_ME_openssl_rand_hex_20"
#   tunnel_credentials = in-cluster 터널(mp-argocd)의 <UUID>.json 내용 그대로(한 줄 JSON).
#     tunnel_id(비밀 아님)는 group_vars/k8s_nodes.yml 의 argocd_webhook_tunnel_id.
argocd_webhook_tunnel_credentials: 'CHANGE_ME_{"AccountTag":"...","TunnelSecret":"...","TunnelID":"..."}'
# 호스트 레벨 백업(etcd·Harbor·Jenkins)의 S3 자격증명 — 컴포넌트별 전용 버킷
#   (mp-etcd-backup-ap2 · mp-harbor-backup-ap2 · mp-jenkins-backup-ap2). 자격증명은 셋 공유.
#   PG barman 이 쓰는 IAM 유저(mp-backup) 키를 재사용하되, 정책이 세 버킷을 커버해야 한다
#   (s3:PutObject/GetObject/ListBucket/DeleteObject). 버킷·리전은 각 role defaults(<role>_backup_s3_bucket).
backup_s3_access_key: "CHANGE_ME_AKIA..."
backup_s3_secret_key: "CHANGE_ME_..."
# etcd at-rest 암호화 키 (Secret 을 etcd 디스크·스냅샷에 암호문으로 저장). 32바이트 base64.
#   생성 = head -c 32 /dev/urandom | base64.  🔴 스냅샷(S3)과 분리 보관 — 잃으면 암호화 스냅샷 복호 불가.
#   group_vars/k8s_nodes.yml 의 etcd_encryption_enabled=true 일 때 필수. 상세 = mp_k8s_backup_strategy.md §7.
#   ↑ 이 "분리 보관"을 실제로 수행하는 게 아래 secrets_backup 롤이다(주1회 묶음 → S3).
etcd_encryption_key: "CHANGE_ME_base64_32bytes"
# 비밀·PKI 묶음(roles/secrets_backup)의 AES-256 passphrase. 생성 = openssl rand -base64 48.
#   🔴 **비밀번호 관리자에도 반드시 따로 저장한다.** 이 파일 자체가 묶음 안에 들어가므로,
#      passphrase 를 여기서만 갖고 있으면 워크스테이션 상실 시 S3 묶음을 영영 못 연다.
#      2026-07-29 묶음이 정확히 그렇게 열 수 없게 됐다(passphrase 파일 소실).
secrets_backup_passphrase: "CHANGE_ME_openssl_rand_base64_48"
