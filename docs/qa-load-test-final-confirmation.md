# 최종 확인 — Account(로그인) 장애가 Chat·MealPlan에 전파되는 구조 (전체 서비스, 로그인 정상화)

> 작성일: 2026-07-21
>
> 배경: 오늘 하루 여러 차례 재실행에서도 결과가 들쭉날쭉했던 문제를, ①원본과 동일 조건(5분·1프로세스) 고정 ②로그인을 실제로 측정에 포함(기존엔 전부 제외) ③로그인이 VUser당 정확히 1회만 일어나도록 캐싱 버그 수정 ④**측정 방법을 Tempo 트레이스 길이에서 nGrinder 원본 응답시간으로 교체**(아래 §0 참고) 네 가지를 다 반영해서 최종 재확인한 결과.

## 0. 방법론 정정 — 왜 Tempo 트레이스 길이를 버렸는가

이전 버전 문서는 Tempo가 계산하는 "트레이스 전체 길이"로 P50/P95/P99를 산출했다. 그런데 크로스서비스 호출이 있는 엔드포인트(MealPlan의 cart·expense-summary가 Account를 호출하는 경우 등)에서, **클라이언트(MealPlan)가 이미 타임아웃으로 응답을 끝낸 뒤에도 서버(Account)가 그 요청을 백그라운드로 계속 붙들고 있으면, 그 시간까지 트레이스 길이에 다 포함된다**는 게 실제 트레이스 span 타임라인 분석으로 드러났다.

**직접 증거** — MealPlan `GET /api/mealplan/cart` 트레이스 하나를 열어보면:
- MealPlan 쪽 root span: **3,009.4ms**에서 끝남 (BudgetProvider의 3.0초 타임아웃 설정과 정확히 일치)
- 그 안에 포함된 Account 쪽 span(`GET /api/users/budget`): **20,077.2ms**까지 계속됨 — MealPlan은 이미 포기했는데 Account는 계속 처리 중이었다

즉 Tempo 기준 "20초"는 사용자가 실제로 기다린 시간이 아니다. **이후 모든 수치는 nGrinder가 직접 측정한 응답시간(클라이언트가 실제로 기다린 시간)을 기준으로 한다** — `output.csv`의 GTest별 2초 구간 `Mean_Test_Time` 중 구간 최댓값.

## 1. 실행 조건

| 항목 | 값 |
|---|---|
| 스크립트 | `full-service-journey.groovy` — OCR 제외(실비용 이슈), 로그인은 VUser(스레드)당 정확히 1회 **측정 포함**. 스레드번호(`grinder.threadNumber`) 키의 static 캐시로 중복 로그인 방지(첫 버전은 인스턴스 필드를 썼다가 반복 로그인 버그 발생 → 수정, §5 참고) |
| 서비스 커버리지 | Recipe·Price·Recipebook·Account(로그인+예산)·Pantry·MealPlan·Notify·Chat — 8개 |
| 비율 | 레시피 탐색 50% / 식단·장바구니 25% / 핫딜·가격 15% / Chat 10% (로그인은 저니 확률과 별개로 스레드당 1회) |
| duration | 5분(300,000ms) — 전 단계 동일 |
| 프로세스 구성 | 1프로세스 × N스레드(N=VU수) |
| 단계 | 25 → 50 → 100 → 200 → 300 → 500VU |

## 2. nGrinder 집계 결과 (로그인 수정판, id=120~125)

| VU | id | Successful | Errors | 오류율 | VM CPU 최대 |
|---:|---:|---:|---:|---:|---:|
| 25 | 120 | 28,839 | 121 | 0.42% | 안전 |
| 50 | 121 | 49,934 | 297 | 0.59% | 안전 |
| 100 | 122 | 71,113 | 792 | 1.10% | 안전 |
| 200 | 123 | 112,326 | 1,816 | 1.59% | 안전 |
| 300 | 124 | 137,084 | 2,791 | 2.00% | 안전 |
| 500 | 125 | 166,022 | 5,016 | 2.94% | 안전 |

VM 전체 CPU는 6단계 내내 위험 수준(80%)에 근접하지 않았다.

### 2.1 로그인 호출 수 검증

| VU | 로그인 실제 호출(추정, CSV 2배 버그 보정) | VU 대비 |
|---:|---:|---:|
| 25 | 25 | 1.0배 |
| 50 | 50 | 1.0배 |
| 100 | 100 | 1.0배 |
| 200 | ~211 | 1.06배 |
| 300 | ~384 | 1.28배 |
| 500 | ~921 | 1.84배 |

낮은 VU(25~100)는 정확히 1:1. 고VU에서 살짝 초과하는 건 로그인 자체가 Account 부하로 실패해서 재시도된 정상적인 현상으로 보인다(캐싱 버그 재발 아님). **애초에 로그인 반복이 문제가 아니었다는 게 아래 §3에서 재확인된다** — 로그인을 거의 정상화했는데도 결과가 똑같거나 더 나쁘다.

## 3. 서비스별 실제 응답시간 (nGrinder 원본, GTest 구간별 Mean_Test_Time 최댓값)

### 3.1 Account — 근본 원인

| VU | account-budget 최대 응답 | 누적 오류 | 컨테이너 CPU 최대(한도 200%) |
|---:|---:|---:|---:|
| 25 | 1,298ms | 30 | 8.9% |
| 50 | 1,618ms | 71 | 20.1% |
| 100 | 1,941ms | 191 | 30.0% |
| 200 | 6,428ms | 422 | 58.3% |
| 300 | **13,309ms** | 640 | **103.6%** |
| 500 | **14,112ms** | 1,117 | **175.6%** |

25VU(가장 낮은 부하)부터 이미 1.3초, 300VU 이후 CPU가 한도(200%)의 절반을 넘고 500VU에서 88%까지 육박한다. 오류도 VU 증가와 함께 계속 누적된다(30→1,117건). **bcrypt 로그인 해싱이 CPU를 다 써버려서 예산조회(`/api/users/budget`)까지 같이 밀리는 CPU 포화 패턴**.

### 3.2 MealPlan — Account 의존 크로스서비스 호출 때문에 느려짐

| VU | mealplan-cart 최대 | expense-summary 최대 | 오류 |
|---:|---:|---:|---:|
| 25 | 17ms | 10ms | 0 |
| 50 | 19ms | 9ms | 0 |
| 100 | 121ms | 111ms | 0 |
| 200 | 866ms | 284ms | 0 |
| 300 | **4,219ms** | **1,168ms** | 0 |
| 500 | **8,120ms** | **4,098ms** | 6 |

200VU까지는 완만하다가 300VU부터 급격히 나빠진다. **오류가 0에 가까운 이유**: `cart`(`services/mealplan/app/routers.py` `get_cart`)와 `expense-summary`는 `asyncio.gather`로 자체 DB 조회와 **Account 예산 API 호출(`BudgetProvider`, 타임아웃 3.0초)**을 병렬 실행하는데, Account가 느려도 타임아웃 후 `budget=None`으로 조용히 성공 응답하기 때문이다 — 겉으론 에러 없이 성공이지만 그 3초 대기가 고스란히 응답시간에 남는다.

### 3.3 Chat — Account 의존 크로스서비스 호출 때문에 느려짐

| VU | chat-assistant 최대 | 누적 오류 |
|---:|---:|---:|
| 25 | 539ms | 14 |
| 50 | 228ms | 30 |
| 100 | 1,673ms | 80 |
| 200 | 548ms | 187 |
| 300 | 456ms | 287 |
| 500 | 373ms | 425 |

MealPlan만큼 규칙적이진 않지만 25VU부터 이미 수백ms대이고 오류가 꾸준히 누적된다(14→425건). **원인**: `services/chat/app/main.py`가 세션 개인화 단계에서 Account의 제외재료 API(`get_excluded_item_ids`, `account_client.py`, 타임아웃 2.0초)를 span 없이 호출하는데, Account가 응답을 못 하면(`PoolTimeout`) 2초를 날리고 빈 리스트로 넘어간다. 실측 로그로 직접 확인된 사실:
```json
{"method":"GET","route":"/api/users/excluded-items","error_type":"PoolTimeout","retryable":false}
```

### 3.4 나머지 서비스(Pantry·Recipebook·Price·Recipe·Notify) — 안정

Account에 의존하지 않는 나머지 5개 서비스는 이번에도 500VU까지 안정적이었다(P99 대부분 50ms 이하, 오늘 앞선 재검증에서 두 차례 독립 재조회로 이미 확인됨 — `docs/qa-load-test-stage4-flow-dbqa.md` 참고).

## 4. 왜 "로그인 반복 호출 때문"이 아닌가

이 문서 작성 전, "로그인이 스레드당 여러 번 반복 호출돼서 Account 수치가 부풀려진 것 아니냐"는 의심이 있었다(§0 이전 버전에서 실제로 발견된 버그). 그래서 캐싱을 수정해 로그인을 거의 1:1로 정상화한 뒤 **똑같은 6단계를 다시 돌렸다**(§2.1). 결과는:

- Account의 최대 응답시간·CPU가 **거의 그대로이거나 오히려 더 나쁘다**(300VU CPU: 이전 121.5% → 이번 103.6%로 비슷, 500VU 응답시간: 이전 계열과 이번 14,112ms로 대등)
- MealPlan·Chat도 여전히 300VU 이후 심각하게 나빠진다

**즉 로그인 반복 호출은 원인이 아니었다.** 로그인 빈도를 실제 서비스처럼(1인 1회) 맞춰도 Account는 진짜로 무너진다.

## 5. 최종 결론

| 서비스 | 원인 | 근거 |
|---|---|---|
| **Account** | CPU 포화(bcrypt 로그인 해싱) + 자체 DB 커넥션 풀 고갈 | 컨테이너 CPU가 500VU에서 175.6%(한도 200%)까지 도달, 응답시간·오류 둘 다 VU에 비례해 계속 악화 |
| **MealPlan** | **자체 문제가 아니라 Account 장애 전파** — `cart`·`expense-summary`가 Account 예산 API를 부르다 3초 타임아웃 | 코드(`asyncio.gather` + `BudgetProvider(timeout=3.0)`) + 실측 응답시간이 타임아웃 값과 정확히 일치 |
| **Chat** | **자체 문제가 아니라 Account 장애 전파** — 제외재료 API를 부르다 2초 타임아웃, 실패 시 `PoolTimeout` | 코드(`account_client.py`, `timeout=2.0`) + 실제 Account 로그의 `PoolTimeout` 에러 |
| Pantry·Recipebook·Price·Recipe·Notify | 없음 | Account에 의존하지 않음, 전 구간 안정 |

**우선순위 1순위는 Account입니다.** Account만 고치면(로그인 CPU 부하 완화 또는 DB 풀 확대) Chat과 MealPlan의 문제도 대부분 같이 해소될 가능성이 높습니다 — 셋이 별개의 3개 병목이 아니라 **Account 하나가 근원이고 나머지 둘은 그 증상**이라는 게 이번 재검증의 핵심 결론입니다.

기존에 알려졌던 "Chat=미인덱스 쿼리", "MealPlan=자체 DB 풀 고갈"은 코드상 실재하는 별개의 문제이지만, 이번 결합 부하 테스트에서 관찰된 심각한 지연의 주된 원인은 그게 아니라 Account 장애 전파였다.

## 6. 참고

- 방법론 정정 전 버전(Tempo 트레이스 길이 기준, 로그인 캐싱 버그 있는 상태) 및 오늘 하루 조사 과정 전체: `docs/qa-load-test-stage4-flow-dbqa.md`
- 기존 단독(서비스별) 재검증 결과: `docs/qa-load-test-backend-retest-results.md`
