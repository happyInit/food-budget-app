# FACTS/agent-PF-resume.md — PF 잔여 11장 판정 원장 (메인 세션 순차, 2026-08-07)

> 범위: PF-01 · 04 · 05 · 06 · 07 · 08 · 09 · 10 · 11 · 12 · 13 (PF-02·03·14 는 `agent-F.md` 완료분).
> 규칙: 카드 1장 판정 즉시 이 파일에 append. 전체 덮어쓰기 금지.
> 웹 조회 사용량: 1/6 (SnapStart — `webcache/docs.aws.amazon.com_lambda_latest_dg_snapstart.html.md` 저장)

## PF-01 SnapStart × 컨테이너 이미지 A-3 조건 불성립 — 판정: 사실

- 검증할 명제: SnapStart 는 zip 전용(컨테이너 이미지 불가)이고 프로비저닝 동시성과 병용 불가 → PoC 조건 A-3 '이미지 동봉 + SnapStart'는 구성 자체가 불가능.
- 행번호 재확인: 전수 일치 — `01_Phase1_일정_AI서버리스.md:82-87`(측정 조건표, L86 `A-3 | 이미지 동봉 | SnapStart 적용`), `03_Phase1_실행_체크리스트.md:39`(A-3 체크박스)·`:44`(랭킹 A-1~A-4 동일 수행), `06_검증_시나리오.md:46`(§1-2 CRF A-3 행)·`:60`(§1-3 LightGBM A-3 행)·`:73-74`(§1-4 챗봇 SnapStart·프로비저닝 행), `05_리스크_대장_롤백기준.md:18`(R-01 대응 순서 ①~⑥)·`:31`(R-02 대응), `02_Phase1_아키텍처_설계.md:145`(§2-4 대응 카드). 정정 없음.
- 공식 문서 확인: https://docs.aws.amazon.com/lambda/latest/dg/snapstart.html [조회 2026-08-07], Supported features and limitations 절 인용:
  - 패키징: "Other managed runtimes …, OS-only runtimes, and **container images are not supported.**"
  - 병용: "**SnapStart does not support provisioned concurrency**, Amazon EFS, Amazon S3 Files, or ephemeral storage greater than 512 MB."
  - 런타임: "Java 11 and later / **Python 3.12 and later** / .NET 8 and later" — 실물 런타임 python:3.12(`02_as_built.md` §1 런타임 행)는 **지원 목록에 있음** → '런타임 미지원' 분기 아님, 쟁점은 패키징으로 좁혀짐.
  - 부가: published version 전용($LATEST 불가) · 리전 = apne2 가용(제외는 NZ·Taipei 뿐) · **Python SnapStart 는 유료**(Java 만 무료 — 캐시 유지비 최소 3h/활성 버전 + 복원 건별 과금).
- CLI 실증: `aws lambda list-functions --region ap-northeast-2` → **AccessDeniedException**(user geonu 에 `lambda:ListFunctions` 없음 — agent-F PF-03 과 동일 IAM 결손). 계정 실증 불가, 판정은 공식 문서로 충분(platform-fact).
- 모델 크기 대조(검증 단계 4): CRF `crf_ingredient.pkl` = 343,657B(`02_as_built.md` §1) → zip 한도(50MB zipped / 250MB unzipped incl. layers — `webcache/...gettingstarted-limits...`) 내 여유. ranker.pkl 은 PVC 상주·크기 [미확인]이나 LightGBM 모델 특성상 zip 대체(A-3′) 성립 가능성 높음 — PoC 당일 실측정.
- ⚠️ 함정 확인: 두 주장 분리 판정 수행 — ① 패키징 제약 = 사실(위 인용, 이것만으로 A-3 붕괴). ② 병용 제약 = 플랫폼 사실이나 **우리 PoC 매트릭스엔 무관**(A-3·A-4 는 별개 조건으로 따로 측정 — 동일 버전에 동시 적용하는 셀이 없음). ③ "지원됨 = A-3′ 유효 측정" 건너뛰기 금지 — pickle 로딩 상태가 스냅샷에 실제로 담기는지는 PF-10 별도 판정(같은 문서 페이지 Compatibility considerations 절이 입력).
- 판정 이유: 명제의 두 하위 주장 모두 공식 문서로 참이고, 핵심 결론(A-3 '이미지 동봉 + SnapStart'는 구성 불가)은 패키징 제약만으로 확정 — 카드의 '사실' 분기 그대로. 병용 제약 절반은 참이되 우리 매트릭스 판정엔 기여하지 않음(위 함정 ②).
- 수정안: 카드 수정안 1~5 채택(런타임 지원 확인됐으므로 'A-3 행 삭제' 분기 아님 — A-3′ 교체 분기). ① `01` L86 을 `| A-3′ | zip 패키징(모델 동봉 또는 S3 다운로드) | SnapStart 적용 |` 로 교체 + 각주 "컨테이너 이미지와 SnapStart 병립 불가 + **Python SnapStart 유료**(캐시 최소 3h/버전·복원 과금) + published version 전용 (조회 2026-08-07, snapstart.html)". ② `03` L39 동일 교체·L44 라벨 정합. ③ `06` L46·L60 셀 교체, L73-74 챗봇 SnapStart 행은 챗봇 패키징 결정에 연동(이미지면 삭제·zip 이면 유지+각주). ④ `05` L18 R-01 사다리를 `① init 로딩 확인 → ② 이미지 동봉 또는 zip+SnapStart(택1·배타) → ③ 프로비저닝 동시성 → ④ SageMaker Serverless → ⑤ Fargate 상주` 로 재번호, 롤백 기준 "④까지"→"③까지". ⑤ `05` L31 R-02·`02` L145 카드는 순차 사다리 유지 가능(단계 간 갈아타기는 병용이 아님)하되 배타 구성 각주 + SnapStart 캐시비를 프로비저닝-vs-Fargate 비용 비교(8/15)에 포함. — 수정 지시 1줄: A-3 를 A-3′(zip+SnapStart)로 전 문서 일괄 교체하고 유료·버전 발행·배타 제약 각주를 달아라(직접 수정은 이 세션 범위 밖 — 지시만).
- 연동 메모: **PF-10·PF-12 판정 시 웹 재조회 금지** — 같은 페이지 캐시(`webcache/docs.aws.amazon.com_lambda_latest_dg_snapstart.html.md`)의 Compatibility considerations(uniqueness·network·temp data)·요금 절을 입력으로 쓸 것(진단서 §2-1 SnapStart 삼형제).

## PF-04 NAT·인터페이스 엔드포인트 시간당 고정비 부재 — 판정: 사실

- 검증할 명제: NAT GW·인터페이스형 VPC 엔드포인트는 데이터 처리 요금과 별개로 시간당(AZ당) 고정 요금이 있고, NAT 1대+엔드포인트 1개 월 고정비만으로 4,577원을 여러 배 초과하는데 문서 11개에 고정비 행이 없다.
- 행번호 재확인: 전수 일치 — `02_Phase1_아키텍처_설계.md:222-224`(§4-1 경고 주석 — NAT "데이터 처리 요금"만 언급)·`:226-229`(§4-2 "NAT 비용 회피"), `05_리스크_대장_롤백기준.md:131-138`(R-11 = "데이터 처리 비용"만, L138 롤백 기준), `06_검증_시나리오.md:189`(§4-1 "NAT 데이터 처리 | 신규 · Vertex 호출량에 비례")·`:195-202`(§4-2 규모별 곡선), `01_Phase1_일정_AI서버리스.md:405`("네트워크 | 0 | NAT 데이터 처리")·`:292`(8/11 D절 "NAT 필요 → 데이터 처리 요금"), `03_Phase1_실행_체크리스트.md:177`(NAT 확인)·`:262`(비용 체크 "NAT 데이터 처리"). 정정 없음.
- 결측 실증: `rg -n -i '고정비|시간당|hourly|730'` → 계획 6개 문서(01·02·03·05·06·08)에서 네트워킹 고정비 히트 **0건** — 전 문서가 NAT 를 변동비(데이터 처리)로만 기술.
- 공식 문서 확인(캐시 — 웹 조회 0회): `webcache/aws.amazon.com_vpc_pricing.html` · `webcache/aws.amazon.com_privatelink_pricing.html` [원 조회 2026-08-07, URL = https://aws.amazon.com/vpc/pricing/ · https://aws.amazon.com/privatelink/pricing/]:
  - NAT: "you are charged for each 'NAT Gateway-hour' that your gateway is provisioned and available" + "charged on an hourly basis. For this region, the rate is **$0.045 per hour**"(예시 리전) + 별도 GB당 처리 요금 — **시간당 고정비 실재**.
  - 인터페이스 엔드포인트: 과금 단위 "**per VPC endpoint per AZ ($/hour)**", 예시 "**$0.01 per hour for each endpoint ENI**" — 고정비 실재.
  - 게이트웨이형(S3): "There are no data processing or hourly charges for using Gateway Type VPC endpoints" — S3 는 고정비 0(함정 절의 뭉뚱그림 방지 확인).
- CLI 실증: `aws pricing get-products`(서울 단가) · `aws ec2 describe-nat-gateways` · `describe-vpc-endpoints` 전부 **AccessDeniedException**(user geonu — pricing:GetProducts·ec2:Describe* 없음). **서울 리전 정확 단가·기존 과금 자원 유무는 [미확인]** — 캐시 HTML 은 리전 단가를 JS 로 로드해 예시 리전 숫자만 담김.
- 월 고정비 계산(예시 리전 단가 = 하한 근사, 환율 1,380원/$ 가정): NAT 1대 $0.045×730h=$32.85 ≈ ₩45,300(**4,577원의 ~9.9배**) / +엔드포인트 1개×1AZ $0.01×730=$7.30 ≈ ₩10,100 → **1AZ 합 ≈ ₩55,400 ≈ 12.1배** / 2AZ(NAT 2대+엔드포인트 2AZ) ≈ $80.3 ≈ ₩110,800 ≈ **24배**. 서울 실단가가 예시 단가보다 낮을 가능성은 사실상 없어 배수 결론은 단가 미확인과 무관하게 성립.
- ⚠️ 함정 확인: ① 단일 AZ 1대 = 합리적 트레이드오프 → 수정안은 "VPC 포기"가 아니라 고정비 행 신설+재판단 트리거로 한정. ② S3(게이트웨이·무료)와 Bedrock(인터페이스·유료)을 분리 기재(위 인용). ③ 크레딧이 청구서를 0으로 보이게 해도 논거는 틀림 — 청구 실측(PF-14 承)과 논거 검증 분리. ④ 결론의 실체는 '8/8 VPC 배치 결정 재개봉' — 02 §4-1 은 'DB 접근' 이유로 7개 함수를 VPC 안에 두는데, 11_실측 §5(NEW-02)의 GCP Cloud Run 분리안이 채택되면 NAT 전제 자체가 바뀜. 단독 확정 금지(팀 결정 연동).
- 판정 이유: ① 고정비 존재 = 공식 요금 페이지(캐시)로 사실. ② 결측 = rg 0건으로 사실. ③ '여러 배 초과' = 하한 계산만으로 ~10배 이상 확정. 명제 전부 성립 — 사실. 잔여 [미확인]은 서울 정확 단가·계정 내 기존 자원뿐이며 결론을 바꾸지 못함.
- 수정안: 카드 수정안 1~6 채택. ① `06` §4-1(L180-192)에 NAT 고정비·엔드포인트 고정비 2행 신설(L189 변동비 행 유지), §4-2 '기타 인프라' 열에 "고정비 포함 — MAU 무관 상수" 각주(규모별 곡선의 부호가 소규모 구간에서 뒤집힘을 명시). ② `01` L405 셀 교체. ③ `05` R-11 제목·'무엇' 확장, L138 롤백 기준을 "고정비+처리비 합계가 월 LLM 토큰 비용 초과 시 VPC 밖 배치 재검토(시한 8/15)"로. ④ `02` §4-1 매트릭스 '고정비 부담' 열 추가, L222-224 에 "시간당 고정 요금(트래픽 무관)" 삽입. ⑤ `02` L227 "NAT 비용 회피" → "NAT 데이터 처리 요금 회피(단, 엔드포인트 자체 시간당 고정비 신규 발생)". ⑥ `08`은 '쓴 만큼만' 문구가 실재하지 않으므로(아래 결함) L96-97·L137 근처에 "네트워킹 계층은 상시 고정비" 단서 병기로 대체. + 서울 정확 단가는 IAM 부여 후 기입(칸 비워두고 '예시 리전 하한' 표기). — 수정 지시 1줄: 비용표 3곳에 고정비 행/열을 신설하고 R-11 을 고정비 포함으로 재정의하라(직접 수정은 세션 범위 밖 — 지시만).
- 진단서 결함(경미): 카드 수정안 6 의 앵커 `08` "'쓴 만큼만 낸다'" 문구는 `08_구두_설명본.md`에 존재하지 않음(rg 0건 — 08 은 L137·L150 에서 이미 비용 단정 회피). 취지 유효하나 대상 문장 부재.
- 연동 메모: **CS-01 판정 시 이 결론 그대로 승계할 것**(같은 주장의 두 판본 — 진단서 §2-1). 8/8 VPC 배치 게이트(PF-06·CS-14·PR-06 묶음)와 11_실측 §5 NEW-02(GCP 분리 시 NAT 소멸) 를 같은 결정 로그에서 처리. 계정 단가 실측은 PF-03 의 'IAM 읽기권한 부여' 태스크에 `pricing:GetProducts`·`ec2:Describe*` 추가로 합침.

## PF-05 Distributed Map 입력·페이로드·과금 축 제약 — 판정: 부분적으로 사실

- (2026-08-07 범위 조정 세션 — 설계 변경급 3장 중 1) 검증할 명제: SF 상태 간 payload 256KB 제한 + DM ItemReader 는 S3 만 지원 → 6,873건 PG 목록은 S3 내보내기 선행 필요(설계에 없음) + Standard/Express 에 따라 오케스트레이션 비용 자릿수 차인데 비용표엔 '상태 전이' 1행뿐.
- 8/8 결정 상태: **미결**(오늘 8/7 — `02:116-127` 자기모순 박스는 8/8 판단 예정). A(그대로 이식) 선택 시 본 카드 무효 조항 유효 — 판정은 B(DM 전환) 전제의 조건부.
- 행번호 재확인: 전수 일치 — `02_Phase1_아키텍처_설계.md:86-102`(§2-3 흐름도)·`:104-114`(개선표+L113 "36,291원 → 21,000원 55% 절감")·`:116-127`(자기모순 박스), `03_Phase1_실행_체크리스트.md:120-123`(DM 정의 체크), `01_Phase1_일정_AI서버리스.md:219`(트리거표)·`:225-232`(B절 재설계), `06_검증_시나리오.md:187`(§4-1 상태 전이 1행 — PF-04 에서 기확인)·`:150-155`(§3-3 배치 실측), `05_리스크_대장_롤백기준.md:86-93`(R-07). 정정 없음.
- 공식 문서 확인(캐시 — 웹 조회 0회): `webcache/docs.aws.amazon.com_step-functions_latest_dg_limits-overview.html.html` · `..._use-dist-map-orchestrate-large-scale-parallel-workloads.html.html` [원 조회 2026-08-07]:
  - payload: "**256 KiB** of data as a UTF-8 encoded string. This quota affects tasks (activity, Lambda …" — 상태 간 256KiB 제한 실재 → 명제 전반부 참.
  - 입력원: "In Distributed mode, you can use either a **JSON payload passed from a** [previous state] … you specify this field only when you use a JSON array or object" + S3 data sources — **'S3 객체만'은 거짓**. JSON 배열 상태 입력 경로가 공식 존재.
  - 인라인 한도: inline mode = "**40 concurrent iterations**" + "**25,000 events** in a single state machine execution history" — 6,873건 × 항목당 이벤트 ≥3~5 ≈ 2.1만~3.4만 → 인라인은 이벤트 한도 초과 위험 + 동시 40 제한. 'DM 과잉' 반론 기각(검증 단계 3 수행).
  - 워크플로 타입: "Distributed mode is **supported in Standard workflows but not supported in Express workflows**" — **부모는 Standard 필수**(신규 확정), child 만 Standard/Express 선택. + "10,000 parallel child workflow executions".
- 실물 대조: 6,873 = `02:113` 실측치(레시피 전량, 템플릿 4,678). 현행 코드 `pipelines/ingest/summarize_reviews.py:90`(증분: `summary IS NULL OR summary_kind='template'`)·`:101`(레시피당 후기 `LIMIT 10`)·`:139`(배치 LIMIT) — 대상 목록은 PG 쿼리, 항목 전달을 ID 로 한정하면 6,873개 ID JSON 배열 ≈ **62KB** [추정: ~9B/ID×6,873] < 256KiB → **S3 내보내기 선행 불필요**. 후기 원문을 상태에 실으면 초과(함정 절 취지 그대로).
- 오케스트레이션 비용: SF 요금 캐시 없음 + 웹 0회 규율 → Standard 전이 단가·Express 단가 **[미확인]**. 산출식만 확정: `6,873 × 항목당 상태 수(≈3) × 단가 × 월 실행 횟수(요약은 주 2회 = 월 8~9회 — 계획의 '매일 03:00' 가정과 실물 주기 상이)`. '비용표 1행뿐' 결측은 사실(`06:187`).
- ⚠️ 함정 확인: ① 256KB 는 상태 payload 에만 — ID-only 설계면 원문 미저촉(위 반영). ② JSON 배열 직접 입력 경로 존재 확인 — S3 필수 주장 기각. ③ 순서 함정 — 8/8 미결 확인 후 판정(조건부 명시). ④ 55% 절감 = LLM 토큰 축, 오케스트레이션 비용 = 별개 축 — 수정안에서 같은 표에 섞지 않도록 행 분리.
- 판정 이유: 참(payload 한도·비용표 결측·인라인 불가) + 거짓('S3 만 지원' → S3 내보내기 필수 주장) 혼재 → 부분적으로 사실. 신규 확정 2건(부모 Standard 필수 · ID 배열 62KB 로 상태 입력 가능)이 설계를 실제로 바꿈: 내보내기 Lambda 불필요, 대신 'ID-only payload' 원칙과 child 타입 결정이 필수 산출물.
- 수정안(8/8 에 B 선택 시에만): 카드 수정안 중 1 은 **대체** — S3 내보내기 삽입 대신 `02` §2-3 에 "(항목 payload = 레시피 ID 만, 총 ≈62KB < 256KiB. 후기 원문은 각 Lambda 가 PG 직조회)" 주석 + "부모 워크플로 = Standard 필수(DM 은 Express 부모 미지원), child = Standard/Express 8/10 확정" 1줄. 2·3·4·5 채택(`03:120-123` 하위에 입력 형식·payload 원칙·child 타입 3줄, `06:187` 3행 분해 — 단가는 [미확인] 칸으로, `06` §3-3 열 추가, `05` R-07 에 DM 한도 행). 6(내보내기 명시)은 불채택. — 수정 지시 1줄: 8/8 결정 로그에 A/B 와 함께 'ID-only payload·Standard 부모' 를 기록하고 B 시 위 항목을 반영하라(직접 수정은 세션 범위 밖 — 지시만).
- 연동 메모: **OP-13 판정 시 이 결론 승계**(같은 결정에 종속 — 8/8 A 선택 시 두 카드 동반 무효). 이전대상 재정의(`../01_이전대상_7R.md`) #4 리뷰 요약 = 이전 대상 확정 — 단 실물 주기는 주 2회(계획 '매일'과 상이)라 비용 산출 시 월 8~9회 기준. 토요일 인계·월요일 착수 일정이면 판단 기준("8/9~8/10 여유")상 **A 가 유력** — 팀장 검토본에 A 권고 근거로 이 카드 인용 가능.

## PF-06 Bedrock 쿼터·Lambda 한도는 계정+리전 공유 — 판정: 부분적으로 사실

- (범위 조정 세션 — 설계 변경급 3장 중 2. 계정 전략 4카드 묶음 PF-06↔OP-10↔CS-14↔PR-06 의 승계 원점) 검증할 명제: Bedrock TPM/RPM 은 계정+리전+모델 단위(함수별 분할 불가), Lambda 동시 실행 한도는 계정+리전 공유 → Bedrock 호출 함수들이 같은 예산을 나눠 쓰고, 8/13 '의도적 쿼터 초과' 테스트가 같은 계정의 프로덕션 챗봇에 429 를 유발.
- 행번호 재확인: 전수 일치 — `03_Phase1_실행_체크리스트.md:87`("환경 분리 전략 확정 (dev/prod 파라미터)")·`:221`("동시성 상한을 쿼터보다 낮게")·`:222`("의도적으로 쿼터를 넘겨 스로틀 동작 확인"), `06_검증_시나리오.md:140-148`(§3-2 표 — L144 nova-micro·L145 sonnet-3.5 행, L148 설정 원칙), `01_Phase1_일정_AI서버리스.md:346-349`(A절 동시성)·`:351-358`(B절 🔴 스로틀 검증), `05:37-46`(R-03)·`02:174·177·178` 은 agent-F(PF-02·03)에서 기확인. 정정 없음.
- 공식 문서 확인(캐시+기존 산출물 — 웹 조회 0회):
  - Lambda: `webcache/...gettingstarted-limits...` — "**concurrent executions** and storage **apply per AWS Region**" + 쿼터 표 `Concurrent executions | 1,000`(계정 단위 — agent-F PF-03 인용 재사용, 함수 단위 아님) → **Lambda 절반 = 사실**(계정+리전 공유, 함수별 예약 동시성은 이 공유 풀에서 차감하는 간접 제어일 뿐).
  - Bedrock: 쿼터 페이지 캐시 없음(삭제됨) — **agent-F PF-03 판정 원장 인용 재사용** [원 조회 2026-08-07, quotas-runtime.html]: TPM 상향은 "**Cross-Region InvokeModel tokens per minute**" 단위(모델별 쿼터 행 개별 존재·Adjustable 여부 모델별) → 쿼터는 계정+리전+**모델** 단위, 함수 단위 쿼터는 존재하지 않음 — **'함수별 분할 불가' = 사실**. 단 모델별 행이 갈려 있어 nova-micro 와 sonnet 은 서로 경쟁하지 않음.
- CLI 실증: `service-quotas list-service-quotas --service-code bedrock`·`lambda get-account-settings` = **AccessDenied**(agent-F PF-03 실측 — 재실행 안 함). 계정 적용값 [미확인] 유지. 계정 단일성: CLI 오류 메시지의 계정 ID `689192361171` 단일 + dev/staging 환경 부재(`02_as_built.md` F-18) → **dev/prod 동일 계정 = 사실**(분리 계정 없음).
- 모델 겹침 확인(함정 절 최우선 지시): 실물 모델 배정(`02_as_built.md` §2) — nova-micro = 감정 배치(07:00)+소비기한 수동+챗봇 '옵션' / sonnet-20241022 = 리뷰 요약(08:00)뿐. **챗봇 라이브 = template, LLM 호출 0**(D-07) → **"프로덕션 챗봇이 429 를 받는다" 서사는 문서 낡음**. 실제 잠식 대상은 챗봇이 아니라 **같은 nova-micro 예산을 쓰는 프로덕션 배치**(감정 분류 — 실서비스 14만 건 경로, 11_실측 §3 🔴).
- ⚠️ 함정 확인: ① 두 주장 분리 판정 완료(둘 다 참 — 단 Bedrock 은 모델 단위라 'Bedrock 호출 4종이 같은 예산' 은 모델 그룹별로만 성립). ② 규모 함정 적중 — MAU 500 실사용(≈0.1 동시, agent-F PF-03 산출)은 어떤 쿼터에도 근접 불가 → **실질 조치는 '8/13 인위 스로틀 테스트의 시간창 격리' 하나로 수렴**, 배분표 정밀화에 반나절 쓰지 말 것. ③ '배치가 챗봇을 굶긴다' 서사는 모델 겹침 확인으로 기각(위).
- 판정 이유: 플랫폼 사실 두 축(Lambda 계정+리전 공유 · Bedrock 계정+리전+모델 단위·함수별 분할 불가)은 참, 그러나 결론부('프로덕션 챗봇 429')는 as-built 로 불성립(챗봇 LLM 0)이고 모델별 쿼터 분리로 '한 예산' 서술도 과장 → 부분적으로 사실.
- 수정안: 카드 수정안 취사 — 1·2 채택하되 **모델 그룹 2행(nova-micro: 감정+소비기한 / sonnet: 요약)으로 축소**, '챗봇 행'은 template 모드 명기 후 제외. 3 채택(핵심): `03` L222 를 "별도 시간창(프로덕션 배치 07:00~08:30 밖, 예: 21:00~22:00)에서만 수행 + 수행 중 같은 모델을 쓰는 배치 없음 확인"으로 교체 — 챗봇 모니터링 대신 **감정·요약 배치와의 시간 겹침 금지**. 4 채택(`03` L87 하위 계정 분리 결정·크레딧 귀속 확인 2줄 — 단 실물 단일 계정이므로 '분리 안 함' 결정을 명시 기록하는 취지). 5·6 채택. — 수정 지시 1줄: 8/13 스로틀 테스트를 프로덕션 배치 시간대 밖으로 옮기고 쿼터 표를 모델 그룹 기준 2행으로 재구성하라(직접 수정은 세션 범위 밖 — 지시만).
- 연동 메모: **OP-10·CS-14·PR-06 판정 시 이 결론 승계**(계정 전략 = 하나의 결정): 실물 = 단일 계정(689192361171)·환경 분리 없음 → 계정 분리는 마감(토 16시 인계) 내 비현실, **시간창 격리가 1차 대응**. 적용값 실측은 PF-03 의 IAM 읽기권한 태스크에 이미 배치됨(중복 신설 금지).

## PF-12 arm64/Graviton 축 부재 + 결정성 미고정 — 판정: 부분적으로 사실

- (범위 조정 세션 — 설계 변경급 3장 중 3) 검증할 명제: Lambda 는 x86_64/arm64 선택 가능·arm64 가 저렴한데 PoC 조건표·비용 곡선에 아키텍처 축이 없고, 아키텍처·라이브러리 미고정 시 '자체 모델 3종 100% 완전 일치'가 무해한 부동소수점 차이로 깨지며, arm64 휠 가용성 미확인.
- 행번호 재확인: 전수 일치 — `01_Phase1_일정_AI서버리스.md:316-328`(8/12 정합성 표 — L324-326 자체 모델 3행 "완전 일치 (100%)", L328 "하나라도 틀리면 즉시 중단"), `06_검증_시나리오.md:94-97`(§2-1 원칙 "자체 모델은 결정론적이므로 100% 일치가 정상")·`:110-112`(8 추출 재료 집합 / 9 추천 순서 / 10 탐지 여부·**점수** — 점수까지 완전 일치 요구), `05_리스크_대장_롤백기준.md:142-149`(R-12, L148 "거의 같으니 괜찮다로 넘기면 안 된다"), `03_Phase1_실행_체크리스트.md:95`(의존성 정리)·`:196-198`(완전 일치 3줄)·`:208`(완료 조건). `01:82-87`·`06:42-49·56-63·195-202` 는 PF-01·04 에서 기확인. 정정 없음.
- 공식 문서 확인(캐시 — 웹 조회 0회): `webcache/aws.amazon.com_lambda_pricing.html` [원 조회 2026-08-07] — 요금표가 "**x86 Price**" / Arm 별도 컬럼 + "x86 and Arm architectures" 병기 + Graviton 절 — **아키텍처별 단가 축 실재**. 서울 정확 단가는 JS 로드라 캐시에 없음 [미확인 — PF-04 와 동일 패턴].
- 원 단위 환산(함정 절 지시): MAU 500 월 Lambda 소요 ≈ 6만 호출×~1s×0.5GB ≈ 3만 GB-s ≈ **$0.5/월 수준**(agent-F PF-03 트래픽 산출 재사용) → arm64 ~20% 절감 시 **월 차액 ≈ ₩100~200 — 현 규모에선 무시 수준**. MAU 50만(×1,000)이면 월 ≈ ₩10만+ 차 — `06` §4-2 곡선 상위 구간에서만 유의미.
- 결정성 실증(레포): ① 핀 수준 — `02_as_built.md` F-09 재사용: "`==` 완전핀 1건뿐·lock 파일 부재 — 재현 빌드 보장 없음" → **런타임 미고정 참**. ② `rg OMP_NUM_THREADS|OPENBLAS|MKL` → **0건**(스레드 수 미고정). ③ `06:112` z-score 행이 '점수'까지 완전 일치 요구 — 실수 비트 동일 요구라 부동소수점 재현성에 취약 참. ④ arm64 휠(python-crfsuite 등) 실다운로드 = 네트워크 접근이라 **미실측**(웹 0회 규율) [미확인]. ⑤ x86↔arm64 diff 실측 = Lambda 미보유·계정 권한 없음으로 미실측.
- ⚠️ 함정 확인: ① 절대액 환산 완료 — "사실이지만 현 규모 무의미" 방향 그대로. ② arm64×SnapStart 병용 가부는 캐시(snapstart.html 2026-08-07 본)에 언급 없음 [미확인] — A-3′(zip+SnapStart) 채택 시 x86_64 고정이 안전 기본. ③ '100% 일치' 완화 위험 — 수정안은 문구 완화가 아니라 **허용 오차 수치 명시**(아래)로 한정, 라벨·순서·불리언은 완전 일치 유지. ④ 관건 패키지는 sklearn-crfsuite 가 아니라 C 확장 **python-crfsuite** — 휠 확인 목록에 명기.
- 이전 대상 재정의 반영(`../01_이전대상_7R.md`): 자체 모델 3종 중 **랭킹 = 이전 제외(#5 flag OFF)** · **CRF = 재정의(#7 배치 전용)** → '3종 100% 일치' 검증 표의 대상 자체가 축소 — 카드 전제 일부는 문서 낡음.
- 판정 이유: (a) 아키텍처 축 부재 = 결측 사실이나 현 규모 절대액 무시 수준(50만 곡선에만 유의미), (b) 결정성 미고정 = 레포 실증으로 사실(무핀+스레드 미고정+점수 완전일치 요구), (c) 휠·diff = 미실측, (d) 대상 3종 중 2종 이전 범위 밖 → 부분적으로 사실.
- 수정안: 실질 채택 2건 — ① **8/8 결정 로그에 'Lambda 아키텍처 = x86_64 고정(Phase 1), arm64 는 Phase 2 검토' 1줄**(카드 수정안 1 의 결정 로그 분기 — PoC 표 열 추가는 불채택, 현 규모 무의미+마감). ② 카드 수정안 3 채택: `03` L95 를 "런타임 고정 목록 산출 — Python 마이너 버전·numpy/scipy/lightgbm/**python-crfsuite** 정확 버전·OMP_NUM_THREADS=1·pickle 프로토콜"로 교체, 8/9 산출물 지정(**아키텍처와 무관하게 필요** — F-09 가 이미 재현 빌드 부재). ③ 카드 수정안 2 축소 채택: 이전 범위에 남는 z-score 만 "탐지 여부(불리언) 완전 일치 + 점수 상대오차 ≤ 1e-9", CRF 는 기존 '추출 집합 일치' 유지, 랭킹 행은 이전 제외로 삭제. ④ 수정안 4 채택(R-12 에 허용 오차 단서). — 수정 지시 1줄: x86_64 고정을 결정 로그에 남기고 런타임 고정 목록을 8/9 산출물로 신설하라(직접 수정은 세션 범위 밖 — 지시만).
- 연동 메모: **OP-05 판정 시 허용 오차 정의(위 ③) 그대로 승계**(한 벌 원칙). PF-01 의 A-3′(zip+SnapStart) 채택 시 x86_64 고정과 정합 — arm64 재론 시 SnapStart 병용 가부부터 확인(캐시 무언급).
