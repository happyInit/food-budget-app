# FACTS — Agent C (pipelines/** · services/{operations,notify,price,pantry})

읽기 전용 as-built 실사. 근거는 전부 코드 원문(`파일:라인`). docs/ 미열람. 2026-08-07.
경로 축약: `PI` = `pipelines/ingest` · `PS` = `pipelines/stream` · `SV` = `services`.

## ① 사실카드

| 주장 | 판정 | 근거 파일:라인 | 확인불가 시 확정 방법 | 뒤집는 기존 주장 |
|---|---|---|---|---|
| 1. 리뷰 요약(#4) 진입점 = argparse CLI 배치. 스케줄은 코드에 없음(수동 `--apply`, 기본 dry-run) | 확인됨 | `PI/summarize_reviews.py:223-234,312-313` | — | "리뷰 요약=배치" 부분 유지 |
| 2. 요약 모델 원문 = `apac.anthropic.claude-3-5-sonnet-20241022-v2:0` (DEFAULT_MODEL), `--compare` 후보 5종(nova-micro·3-haiku·haiku-4.5·3.5-sonnet-v2·sonnet-4.5) | 확인됨 | `PI/summarize_reviews.py:50-57` | — | "리뷰 요약=sonnet-3.5" 확인 |
| 3. 요약 대상 = **증분**: `s.summary IS NULL OR summary_kind='template'` + 리뷰 ≥ MIN_REVIEWS(=10). `--redo` 시에만 전량 | 확인됨 | `PI/summarize_reviews.py:71,86-94,240` | — | — |
| 4. 요약 저장 = **UPDATE만**(upsert 아님). 집계행(감정분류 산출) 없으면 미저장·skip 카운트. 템플릿을 'llm'으로 승격, `summary_model` 전용 컬럼 | 확인됨 | `PI/summarize_reviews.py:147-152,291-301` | — | — |
| 5. 요약 재시도·중단 = 건별 try/except로 스킵(배치 비중단), 재시도 루프 없음 — 실패분은 다음 실행이 증분 쿼리로 재포착 | 확인됨 | `PI/summarize_reviews.py:263-268` | — | — |
| 6. 10건 미만 집계 템플릿 = `PI/fill_summary_template.py` — LLM 0호출, 감정 라벨 집계 문구. 경계값을 `summarize_reviews.MIN_REVIEWS` import로 공유(상호 덮어쓰기 방지) | 확인됨 | `PI/fill_summary_template.py:31-48,51` | — | — |
| 7. 감정 분류(#8, F-03) = **Kafka 컨슈머 아님**. argparse 배치 + boto3 `bedrock-runtime.converse`, 20건/호출 배치 프롬프트 | 확인됨(배치) | `PI/score_review_sentiment.py:39,152-161,140-149` | — | "감정 분류=스트림/배치(Nova)" 중 **스트림 반증**, Nova·배치 확인 |
| 8. 감정 모델 원문 = `apac.amazon.nova-micro-v1:0`, REGION=`ap-northeast-2`(데이터 레지던시 주석) | 확인됨 | `PI/score_review_sentiment.py:37-38` | — | — |
| 9. 감정 대상 = **증분**(`LEFT JOIN recipe_review_sentiment ... WHERE s.review_id IS NULL`) — 전량 아님. 중단 후 재개가 기본 동작 | 확인됨 | `PI/score_review_sentiment.py:42-48,14` | — | — |
| 10. 감정 저장 = `INSERT ... ON CONFLICT (review_id) DO NOTHING` + 레시피 집계 `recipe_review_summary` upsert. **배치마다 커밋**(중단해도 진행분 보존) | 확인됨 | `PI/score_review_sentiment.py:50-72,187-190` | — | — |
| 11. 감정 영구 미분류 실패모드: temperature=0이라 어휘 밖 라벨은 재실행해도 동일 → `discarded_labels` 전역 집계로 노출(16건 실측 사고 후속) | 확인됨 | `PI/score_review_sentiment.py:99-106,203-208` | — | — |
| 12. F-12: z-score 윈도우 상태 = **저장된 상태 없음** — 매 실행 `retail_price` 30일 윈도우를 `_DAILY_SQL`로 풀스캔 재계산(순수함수 `detect()`) | 확인됨 | `PI/detect_price_anomaly.py:70-108,140-178` | — | "가격 알림=상태 없는 배치" — **탐지 계산은** 무상태 맞음 |
| 13. 단, matview `retail_unit_price`는 이상탐지와 **무관**(Price 서비스 조회 성능용, #186). 탐지는 원시 `retail_price`를 직접 읽음 | 확인됨 | `PI/migrate_price_matview.py:1-18`, `PI/refresh_price_matview.py:1-13`, `PI/detect_price_anomaly.py:94-95` | — | — |
| 14. 발행·억제는 **PG 상태 3종**에 의존: `price_baseline` upsert(as_of별) · `price_anomaly` upsert+`published_at`(미발행 재시도 표시) · 성숙도 게이트 N≥28 발행 억제 | 확인됨 | `PI/detect_price_anomaly.py:189-256,44-46,313-324` | — | "상태 없는 배치" **부분 반증**(억제·재시도 상태는 PG) |
| 15. 발행 = Kafka `price.anomaly.detected`, key=item_id, 멱등키 `anomaly_id={item_id}:{source}:{observed_at}` + `anomaly_db_id`(FK용). flush 미전달 시 `DeliveryIncomplete` 예외(기본 dry-run, `--emit` 필요) | 확인됨 | `PS/produce_price_anomaly.py:45-88,91-107`, `PI/detect_price_anomaly.py:267-269` | — | — |
| 16. F-07: 알림 중복 억제 = **PG, Redis 아님**. 2중: ① 7일 쿨다운 = `notify.notification`의 `payload->>'item_id'`+`created_at` NOT EXISTS(전용 표현식 인덱스 `notification_lowprice_cooldown_idx`) ② `price_alert_sent` PK(anomaly_id,user_id) — 재전달 방어 | 확인됨 | `PS/consume_price_anomaly.py:46-106`, `PI/migrate_lowprice_cooldown_idx.py:1-10` | — | 쿨다운 저장소=PG로 확정 |
| 17. fan-out 컨슈머 = `PS/consume_price_anomaly.py`, GROUP=`price-anomaly-notifier`. `notify.notification`의 **첫(유일) writer**. INSERT...SELECT 단문으로 경쟁 제거, FK 사전 EXISTS 가드(크래시루프 실측 후속) | 확인됨 | `PS/consume_price_anomaly.py:1-8,34,40-46,71-80` | 라이브 `mp-price-anomaly-notifier`와의 대응은 `grep -rn price-anomaly consume_price deploy/ infra/` (매니페스트=타 에이전트) | — |
| 18. 핫딜 = 별도 경로(이상탐지와 분리): `retail.deal.raw` → `deal-notifier`(`PS/consume_deal.py`) → PG(deal_type/timedeal_end)+**Redis** ZSET `retail:deals:active`(score=마감 epoch)+HASH detail. **알림 fan-out 없음** — API 조회면. Redis 불가 시 PG만(graceful) | 확인됨 | `PS/consume_deal.py:1-5,127-139`, `PS/_redis.py:1-43` | 라이브 `mp-deal-notifier` 대응 확인은 위와 동일 방법 | — |
| 19. 딜 만료 정리 = `PS/prune_deals.py`(Redis만, PG 이력 보존), "CronJob 10분/`--loop 600`" 주석 | 확인됨 | `PS/prune_deals.py:1-5`, `PS/README.md:31` | — | — |
| 20. 기능#10 `draft_shelf_life.py` = **소비기한 초안 구조화 추출**(품목명→`{ROOM,FRIDGE:{min,max}}` JSON), 모델 `apac.amazon.nova-micro-v1:0`, 트리거=수동 argparse(`--apply`, 기본 dry-run). `source='AI_DRAFT'`·상한 캡·FREEZER 제외·신선 8분류 한정·건조품 정규식 제외 | 확인됨 | `PI/draft_shelf_life.py:37-57,77-115,161-171` | — | "구조화 추출(Nova)" = Nova(micro)로 확인 — 단 대상은 리뷰가 아니라 소비기한 |
| 21. Kafka 토픽 5종: `retail.crawl.raw`(3p) · `retail.deal.raw`(2p) · `recipe.crawl.raw`(3p) · `events.user.activity`(3p) · `price.anomaly.detected`(1p). retention 7d delete. +DLQ `<topic>.dlq` | 확인됨 | `PS/_topics.py:9-17`, `PS/create_topics.py:11-25`, `PS/_dlq.py:50` | — | "Kafka는 Phase 2 범위 밖" **반증** — 알림 체인 필수 경로 |
| 22. 라이브러리 = confluent-kafka(librdkafka). 프로듀서: `enable.idempotence`+`acks=all`+linger 50ms. 컨슈머: **수동 커밋**(`enable.auto.commit=False`), `auto.offset.reset=earliest`, cooperative-sticky | 확인됨 | `PS/_kafka.py:15-34` | — | — |
| 23. 오프셋 커밋 순서 = PG `conn.commit()` **성공 후** `c.commit()`, COMMIT_EVERY=100(retail만 200) 또는 idle 시 — at-least-once + DB 멱등 upsert | 확인됨 | `PS/consume_user_event.py:88-102,159-162`, `PS/consume_retail.py:26` | — | — |
| 24. 컨슈머 그룹 5종(전부 `pipelines/stream`에 실재): `retail-refiner`(consume_retail) · `recipe-refiner`(consume_recipe) · `deal-notifier` · `user-event-sink`(consume_user_event) · `price-anomaly-notifier` | 확인됨 | `PS/consume_retail.py:25`, `PS/consume_recipe.py:25`, `PS/consume_deal.py:25`, `PS/consume_user_event.py:25`, `PS/consume_price_anomaly.py:34` | — | — |
| 25. 프로듀서: ① 크롤러 `--kafka` 직접(평상시) ② `produce_retail.py`/`produce_recipe.py` = 파일 리플레이/폴러 ③ `detect_price_anomaly --emit` ④ `produce_user_event.py` = 백엔드 인라인 헬퍼 — **단 services/* 에서 실호출 0건**(테스트만) | 확인됨 | `PS/README.md:5-16,27`, `PS/produce_user_event.py:1-11` + grep(services 전체에서 emit_user_event 호출 없음) | 프론트/타 레포 발행 여부: `grep -rn events.user.activity` 전 레포 + Kafka `kafka-consumer-groups --describe` | — |
| 26. user-event-sink 역할 = `events.user.activity` → `activity.user_event` INSERT, `event_id` UNIQUE `ON CONFLICT DO NOTHING`(멱등). VIEW·ADD_CART·NOTIF_CLICK만 유효 | 확인됨 | `PS/consume_user_event.py:1-6,30-37,58-62` | — | — |
| 27. DLQ = 예외 유형 화이트리스트(영구→`<topic>.dlq`+오프셋 전진 / 일시·미지→raise), 레코드별 savepoint, 재투입 `replay_dlq.py`(오프셋 미커밋·원본 그대로) | 확인됨 | `PS/_dlq.py:60-89,92-109`, `PS/replay_dlq.py:1-23` | — | — |
| 28. 클릭스트림 재수집: PG `activity.user_event`가 정본·Kafka는 트랜스포트(7d). 180일 후 `recipe_popularity` 집계 승격 후 **원문 삭제**(같은 트랜잭션) → 보존창 밖 원본 재수집 불가 | 확인됨 | `PS/create_topics.py:20-21`, `PS/prune_user_data.py:26-47,65-72,89` | — | "클릭스트림 재수집 불가" 정합(코드 근거로 지지) |
| 29. notify 발송 수단 = **없음**. FCM·웹푸시·APNs 코드/의존성 0건 — 인앱 알림함(GET 목록 + PATCH 읽음)뿐. 푸시는 미구현 | 미구현(발송) | `SV/notify/app/routers.py:15-38`, `SV/notify/app/main.py:43-52` + grep(fcm/webpush/push 0건) | — | — |
| 30. notify 호출자 = 프론트(읽기 API)뿐. 알림 **생성**은 notify API를 경유하지 않고 컨슈머가 `notify.notification`에 직접 INSERT(스키마-퍼-서비스 경계를 파이프라인이 관통) | 확인됨 | `PS/consume_price_anomaly.py:5-8,46-60` | — | — |
| 31. 재고 반영 경로(#7) = OCR 엔진(`services/ocr`, C 소유 밖)→프론트 HITL 확정→`POST /api/pantry/receipts`(**HTTP, Kafka 아님**)→한 트랜잭션: `ocr_receipt(+item 전줄)` 저장 + keep·식품·storage 있는 줄만 `pantry_item` INSERT(source='OCR') | 확인됨 | `SV/pantry/app/routers.py:97-148`, `SV/pantry/app/queries.py:145-166` | OCR 엔진 상세는 `services/ocr/` 실사(타 소유) | — |
| 32. 누적 연산 **아님** — 영수증 줄마다 새 `pantry_item` 행 INSERT(수량 증감/병합/upsert 없음). 코드에 영수증 멱등 장치 없음 → 같은 확정 재전송 시 중복 행 | 확인됨(코드층) | `SV/pantry/app/queries.py:17-27`, `SV/pantry/app/routers.py:130-133` | DB층 UNIQUE 여부: `psql \d pantry.ocr_receipt*` 또는 `grep -n 'unique' docs/prd/schema-production.sql` | — |
| 33. 순서 의존성: `expire_at`은 **등록 시점 1회** 계산(`estimate_expire_date` = 담은날+days_max, min 폴백) — 참조표가 늦게 채워지면 NULL 고착. 이를 `PI/recompute_pantry_expire.py`가 따라잡음: `expire_at IS NULL`만·결과가 미래인 것만·멱등, UPDATE에 조건 재확인(경쟁 방어) | 확인됨 | `SV/pantry/app/estimate.py:12-18`, `PI/recompute_pantry_expire.py:1-10,51-78` | — | — |
| 34. operations의 z_score(`anomaly_analyzer.py`) = **가격과 별개** — Prometheus 메트릭 시계열(rolling baseline+MAD+변화율, 연속 위반 창) + Alertmanager 웹훅 인시던트 상관. 소유=인프라 관측 도메인 | 확인됨 | `SV/operations/app/anomaly_analyzer.py:33-78`, `SV/operations/README.md:1-24`, `SV/operations/app/prometheus_collector.py:1-30` | — | — |
| 35. 날짜 경계(F-17): 가격탐지만 KST 명시(`crawled_at AT TIME ZONE 'Asia/Seoul'::date` — UTC date 절단 시 KST 2일 혼입 실측 주석). 리뷰 배치들 = 날짜 조건 자체가 없음(존재 여부 증분). pantry = `date.today()`(프로세스 로컬)·`current_date`·`date_trunc('month', closed_at)`(DB 세션 TZ 의존, KST 미명시). prune_user_data cutoff = UTC aware. 월 집계 위치 = `pantry_stats`(month=closed_at 기준) | 확인됨(혼재) | `PI/detect_price_anomaly.py:73-84,91`, `SV/pantry/app/routers.py:87`, `SV/pantry/app/queries.py:65,110-130`, `PS/prune_user_data.py:89` | DB 세션 TZ 실값: `psql -c 'show timezone'` | — |
| 36. DB 접속: `PI/_db.py` = **psycopg3 sync 단일 커넥션**(풀 없음), 기본 `192.168.0.8:5432/foodbudget/fbapp`, env PG*로만 재지정 — Pooler 경유는 코드에 없음(주입 문제). services = `AsyncConnectionPool` + `row_factory=dict_row` + **`prepare_threshold=None`(PgBouncer/pooler 대응)** + check_connection. ingest에는 prepare 비활성이 없음 → transaction-pooler 직결 시 위험 요소 | 확인됨(상이) | `PI/_db.py:15-22`, `SV/notify/app/db.py:20-34`, `SV/price/app/db.py:11-25` | 실배선(pooler 여부): 배포 매니페스트의 PGHOST env 확인(타 에이전트) 또는 `kubectl get cm,secret -n <ns> | grep -i pg` | — |
| 37. 코드 내 스케줄 근거(매니페스트 제외): retail 폴 "일1~2회 CronJob"·recipe "주1회"·타임세일 15시·마감세일 17시·deal-pruner 10분·user-data-pruner 일1회 — 전부 주석/README. 크롤 실측 시각 = 컬리 03:30 KST·오아시스 04:10/13:10 KST. matview refresh = "폴 윈도우(11~12시·17~18시) 뒤 cron". **LLM 배치 3종(요약·감정·소비기한)과 이상탐지는 코드에 스케줄 없음(수동)** | 확인됨 | `PS/produce_retail.py:1-2`, `PS/produce_recipe.py:1`, `PS/README.md:76-80,31`, `PI/detect_price_anomaly.py:75-77`, `PI/refresh_price_matview.py:8-10` | 실제 주기: CronJob 매니페스트(타 에이전트) | — |
| 38. 수량 대조(#12): 리뷰 총 141,298건(`recipe_review` 실측 주석) — 감정 모집단 = 그 전량의 미분류분. 분류완료 141,282 주석(차이 16=버려진 라벨 사고). **6,873** = 리뷰 보유 레시피 수(≥5건 3,086 + <5건 3,787 주석 합) — 요약 모집단과 정합. 현행 요약 대상은 MIN_REVIEWS=10 → 2,195개(91.4% 커버 주석), 템플릿 대상 4,678개(1~9건) | 확인됨(주석 기준) | `PI/score_review_sentiment.py:8`, `PI/summarize_reviews.py:11-12,62-71,114`, `PI/fill_summary_template.py:3-4` | DB 실측: `select count(*) from recipe_review;` 등 | — |
| 39. ES(#13): `PI/index_recipes_es.py` = PG `recipe`+`recipe_ingredient` → ES `recipes` 인덱스(nori), **drop→recreate 전량 재색인**·자동 훅 없음(수동). 게이트 = source='10K'+strict 미매칭 0. PGSync는 **별도 CDC 경로**(`recipes_pgsync` 인덱스) — 같은 게이트가 `deploy/pgsync/plugins/recipe_servable.py`에 중복 존재, 한쪽만 고치면 어긋난다는 경고 주석 | 확인됨 | `PI/index_recipes_es.py:1-12,58,66-87`, `PI/_db.py:25-35`(ES 인증 env 선택) | — | — |
| 40. 로깅·메트릭(#14): stream = 구조화 JSON 로그(`service=data-pipeline`, 필드 화이트리스트) + Prometheus `fb_pipeline_*` 5종+(METRICS_PORT 시 HTTP 노출). **ingest 배치 = print만, 메트릭 0**(예외: index_recipes_es는 구조화 로그 사용) — LLM 배치 관측은 stdout 의존 | 확인됨 | `PS/_observability.py:35-71`, `PS/_metrics.py:13-65`, `PI/summarize_reviews.py:311`, `PI/score_review_sentiment.py:198` | — | — |

## ② as-built 요약

- [확인됨] 기능#4·#8·#10은 전부 **수동 argparse 배치**(코드 내 스케줄 없음, 기본 dry-run): 요약=claude-3-5-sonnet-v2(증분, UPDATE 승격, 템플릿 폴백=`fill_summary_template`), 감정=nova-micro(증분 LEFT JOIN, ON CONFLICT 멱등), 소비기한 초안=nova-micro 구조화 JSON(AI_DRAFT·FREEZER 제외). 가격 체인 = 탐지 배치(30일 윈도우를 매번 PG 재계산, matview 무관) → Kafka `price.anomaly.detected` → `price-anomaly-notifier` 컨슈머 → `notify.notification` 직접 INSERT. F-07 중복 억제 = **PG 2중**(7일 쿨다운 표현식 인덱스 + `price_alert_sent` PK), Redis 아님. 재고 반영 = HTTP HITL(`POST /receipts`) 단일 트랜잭션 INSERT(누적 아님), NULL expire 따라잡기 = `recompute_pantry_expire`. operations z_score = 인프라 메트릭용, 가격과 별개.
- [불일치] 기존 주장 대비: "감정 분류=스트림" → **배치**(F-03 반증) · "가격 알림=상태 없는 배치" → 탐지 계산은 무상태이나 **발행·억제·재시도는 PG 상태 3종 의존** · "Kafka는 Phase 2 범위 밖" → **토픽 5종+컨슈머 그룹 5종+DLQ가 실가동 코드**이고 가격 알림·클릭스트림·수집 전부 Kafka 경유 · notify는 "발송" 서비스가 아니라 읽기 전용 알림함(푸시 미구현) · `produce_user_event` 헬퍼는 services 코드에서 실호출 0건(클릭스트림 발행 주체는 이 레포 백엔드에 미배선).
- [미확인] 라이브 `mp-price-anomaly-notifier`·`mp-deal-notifier`와 코드의 1:1 대응 및 실스케줄·Pooler 경유 여부(매니페스트=타 에이전트 소유) · pantry 영수증의 DB층 UNIQUE 유무(schema-production.sql 확인 필요) · DB 세션 TZ 실값(`show timezone`) — 각 행 4열의 명령으로 확정 가능.
