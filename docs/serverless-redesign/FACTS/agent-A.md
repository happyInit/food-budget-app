# FACTS/agent-A — ocr·video·ml/video-recipe as-built 실사 (2026-08-07)

읽기 전용 실사. 근거는 전부 로컬 워킹트리(`feat/ai-vertex-k8s-wiring`) 기준 `파일:라인`.
판정 어휘 = 확인됨 / 반증됨 / 미구현 / 확인불가.

## ① 사실카드

| 주장 | 판정 | 근거 파일:라인 | 확인불가 시 확정 방법 | 뒤집는 기존 주장 |
|---|---|---|---|---|
| 1-1. 기능#1 진입점 = HTTP `POST /api/pantry/ocr`(FastAPI 라우트) — 이벤트/메시지 트리거 아님 | 확인됨 | services/ocr/app/main.py:123-125 | — | "영수증 인식=이벤트 기반" |
| 1-2. 기능#1은 202 접수 → `GET /api/pantry/ocr/{job_id}` 폴링(프론트 1s 간격) 비동기 | 확인됨 | services/ocr/app/main.py:117-120,128-133 · frontend/src/lib/queries.ts:364 | — | — |
| 1-3. 비동기 실행체 = **인프로세스** `asyncio.create_task` — 큐·워커·이벤트버스 없음(코드에 TODO로 명시) | 확인됨 | services/ocr/app/main.py:118-119 (`# TODO(백엔드): BackgroundTasks/큐 … 스켈레톤은 즉시 태스크`) | — | "비동기=큐/이벤트 소비" |
| 1-4. 호출 그래프: `upload_receipt`→`_accept`→`_run_job`→`process_image`→`VisionBackend.parse`→`google-genai generate_content` **1회** | 확인됨 | services/ocr/app/main.py:81 · app/pipeline/process.py:14 · app/pipeline/backend/vision.py:189-192 | — | — |
| 1-5. 영수증 원본 이미지는 메모리 bytes로만 처리, 어디에도 영속 저장 안 함(잡스토어엔 파싱 JSON만) | 확인됨 | services/ocr/app/main.py:111,119 · app/store.py:37-44 | — | — |
| 9-1. 기능#9 품목 분류는 #1과 **한 호출** — Gemini는 품목별 `is_food` 불린만 내고, category/storage/in_expense는 로컬 규칙+gazetteer 캐스케이드(LLM 아님) | 확인됨 | services/ocr/app/pipeline/backend/vision.py:34 · app/pipeline/process.py:15-23 · app/pipeline/classify.py:273-309 | — | "품목 분류=내부 처리(Gemini)" — Gemini 별도 호출 없음 |
| 9-2. 미해결 품목용 LLM 분류 티어(7단)는 "훅만, 아직 미배선" | 미구현 | services/ocr/app/pipeline/classify.py:9,306-309 | — | — |
| 2-1. 기능#2 진입점 = `POST /api/recipes/extract` 202 → `GET /api/recipes/extract/{job_id}` 폴링(프론트 2s) | 확인됨 | services/video/app/main.py:173-197,200-206 · frontend/src/lib/queries.ts:380 · frontend/src/pages/YoutubeExtract.tsx:10-16 | — | — |
| 2-2. 장시간 처리 = FastAPI `BackgroundTasks` + `asyncio.to_thread`, 전체 `wait_for` **120s 상한** — 큐 없음 | 확인됨 | services/video/app/main.py:196,108-117 · app/config.py:20 | — | "영상→레시피=장시간(별도 큐 필요 규모)" — 실측 상한 120s 인프로세스 |
| 2-3. `ml/video-recipe`는 **라이브러리** — 같은 프로세스에 `sys.path` 삽입 import(컨테이너엔 `/opt/video-recipe` COPY, `VIDEO_LIB_PATH`) | 확인됨 | services/video/app/main.py:34-37,88-89 · services/video/Dockerfile:15-16 | — | "별도 프로세스/서비스" |
| 2-4. 중복 방지 = Redis 단일비행 락(180s) + 교차유저 결과 캐시(30일, 히트 시 Gemini 호출 0) | 확인됨 | services/video/app/store.py:18-20,58-63 · app/config.py:28-29 · app/main.py:187-193 | — | — |
| 2-5. 재분석 = 하드실패(H2~H5) 시 상위 모델 **1회 한정**, H0(영상 미수신)은 재분석 없이 즉시 실패 | 확인됨 | ml/video-recipe/pipeline.py:113-124 | — | — |
| 2-6. refine(정제) 단계 = 설정·stage 어휘만 존재, 파이프라인에 호출 배선 없음 | 미구현 | ml/video-recipe/extract.py:111-112 · pipeline.py(refine 호출 0건) · services/video/app/config.py:18-19 | — | — |
| 10-1. 기능#10 "구조화 추출"은 별도 단계가 아님 — OCR·영상 모두 **단일 Gemini 호출**이 JSON 스키마 강제(ocr는 `response_mime_type=application/json`)로 구조화까지 수행 | 확인됨 | services/ocr/app/pipeline/backend/vision.py:25-41,170 · ml/video-recipe/extract.py:17-23,97-107 | — | "구조화 추출=내부 처리(Nova)" |
| 10-2. Nova/Bedrock 경유 호출 존재 | 반증됨 | services/video/app/main.py:9-10(Bedrock 명시 기각 — YouTube URL 입력 불가) · requirements.txt:8 · 양 서비스 코드에 bedrock/nova 참조 0건 | — | "구조화 추출=Nova" |
| 5-1. 외부 LLM 인증 **기본값 = 개인 Gemini API 키**(`genai.Client(api_key=…)`) — ocr `genai_backend="api_key"`, video `VIDEO_GENAI_BACKEND` 기본 `api_key` | 확인됨 | services/ocr/app/config.py:23,27 · app/pipeline/backend/genai_client.py:95-98 · ml/video-recipe/extract.py:86,96 · deploy/app/docker-compose.yml:236(GENAI_BACKEND 기본 api_key)·VIDEO_GENAI_BACKEND 기본 api_key(video environment 블록) | — | "AI 인증=Vertex SA 키" |
| 5-2. Vertex 경로는 env 토글로 **존재**(`GENAI_BACKEND=vertex` + `GCP_PROJECT_ID`/`GCP_LOCATION` + `GCP_SA_KEY_JSON` 원문 env 또는 ADC) — 기본 아님, 롤백 전제 설계 | 확인됨 | services/ocr/app/pipeline/backend/genai_client.py:79-93 · app/config.py:27-34 | — | — |
| 5-3. 로컬 `services/ocr/.env` 실측 = `GEMINI_API_KEY` 설정 + PG 5종뿐, `GENAI_BACKEND` 미설정 → api_key 모드(키 값은 미열람·미기록) | 확인됨 | services/ocr/.env(키 이름만 확인: GEMINI_API_KEY·PGHOST·PGPORT·PGDATABASE·PGUSER·PGPASSWORD) | — | "Vertex SA 키 운영 중" |
| 5-4. **K8s 라이브** 파드가 실제로 api_key/vertex 어느 모드인지(시크릿 `mp-ocr-secrets` 실값) | 확인불가 | deploy/k8s/에 README.md만 존재(매니페스트는 mealplanning-config 레포로 이관 — 커밋 c3cb195) | `kubectl -n fb-app-ai get deploy -o yaml \| grep -A2 GENAI_BACKEND` · `kubectl get externalsecret -A \| grep ocr` · mealplanning-config 레포에서 `grep -rn "GENAI_BACKEND\|VIDEO_GENAI_BACKEND"` | — |
| 5-5. `config_canary.py` 정체 = **Vertex 전환 카나리 배포가 아니라 모델 드리프트 카나리** — 운영 config(system_instruction·response_mime_type·thinking_config) 수용 여부만 주기 점검, `--backend vertex` 옵션 보유. 트래픽 분할 없음 | 확인됨 | services/ocr/app/config_canary.py:2-12,34-38,94-108,113 | — | "config_canary=Vertex 카나리 전환 중" |
| 5-6. 카나리의 운영 스케줄 실행체(K8s CronJob) 실재 여부 | 확인불가 | docker-compose.yml:129-134(주석: `deploy/k8s/ocr-config-canary.yaml`로 이관) — 해당 yaml이 로컬 트리에 없음 | `kubectl get cronjob -A \| grep -i canary` · mealplanning-config 레포에서 `ocr-config-canary` 검색 | — |
| 6-1. 런타임 코드 모델 ID 전수 = `gemini-3.5-flash-lite`(ocr config.py:40 · video config.py:16 · extract.py:77 · pipeline.py:98), `gemini-3.5-flash`(video config.py:17,18 · pipeline.py:120) — 이 6곳 외 런타임 하드코딩 없음 | 확인됨 | services/ocr/app/config.py:40 · services/video/app/config.py:16-18 · ml/video-recipe/extract.py:77 · pipeline.py:98,120 | — | — |
| 6-2. 설정/배포 기본값 모델 ID = `GEMINI_MODEL=gemini-3.5-flash-lite`(.env.example:11, compose:227) · `VIDEO_EXTRACT_MODEL` 기본 lite·`VIDEO_RETRY_MODEL` 기본 flash(compose video env 블록). 테스트에만 `gemini-pro` 문자열(test_genai_client.py:13, backend명 오용 테스트) | 확인됨 | services/ocr/.env.example:11 · deploy/app/docker-compose.yml:227 및 video environment 블록(VIDEO_EXTRACT_MODEL/-RETRY) · services/ocr/tests/test_genai_client.py:13 | — | — |
| 6-3. 리전 문자열 하드코딩 = **런타임 코드에 없음.** `GCP_LOCATION`은 env 전용·기본값 의도적 부재(데이터 레지던시). `global`·`asia-northeast3`·`us-central1`은 주석/에러메시지/테스트에만 등장 | 확인됨 | services/ocr/app/config.py:28-31 · app/pipeline/backend/genai_client.py:84-89 · .env.example:19-22(채택 리전=global은 문서적 결정) | — | — |
| 13-1(F-13). 품목 부분 실패 저장 = 품목을 버리지 않음 — 읽기 실패 필드는 null 유지 + `needs_review` 플래그로 **전 품목 저장**(비-dict 항목만 skip) | 확인됨 | services/ocr/app/pipeline/backend/vision.py:40(프롬프트: 못 읽으면 raw_text만)·127-138 · app/models.py:15-27 | — | — |
| 13-2(F-13). 사용자 표시 = HITL 편집표에서 needs_review 줄 주황 하이라이트+`확인` 배지, 유저가 수정 후 확정. 확정 후 가격 미인식 있으면 `needs_expense_review` 경고 문구 | 확인됨 | frontend/src/components/forms/OcrFlow.tsx:224,231,119-121,201-203 | — | — |
| 13-3(F-13). 합계 정합 검증(ocr 서비스 내) = `total_amount` vs Σ품목 비교 로직 **없음** — `realign_prices`는 가격 오정렬 1쌍 스왑 시 "합계 보존"만 하지 검증이 아님 | 미구현 | services/ocr/app/pipeline/classify.py:316-340 · app/main.py:79-100(검증 부재) | — | "합계 정합 검증 존재" |
| 13-4(F-13). 합계 앵커 로직은 **확정 단계(pantry 서비스, 본 실사 소유 밖)** 계약에 존재 시사 — 응답 필드 `expense_basis: 'total_anchor'\|'line_sum_fallback'\|'selected_items'` | 확인됨(프론트 계약 한정) | frontend/src/lib/api.ts:570 | pantry 구현 확인: `grep -rn "total_anchor" services/pantry/` | — |
| 20-1(F-20). 영수증 원본 업로드 목적지 = **MinIO도 S3도 아님** — 프론트가 multipart로 OCR 서비스에 직접 전송, 서비스는 저장 없이 처리. 오브젝트 스토리지 참조 0건 | 확인됨 | frontend/src/lib/api.ts:509-515 · services/ocr/app/main.py:110-119 · ocr/video 코드 grep minio·s3·bucket 0건 | — | "영수증 원본=MinIO/S3 업로드" |
| 20-2(F-20). 프론트 '원본 미리보기'는 로컬 `URL.createObjectURL`(브라우저 메모리) — 업로드와 무관 | 확인됨 | frontend/src/components/forms/OcrFlow.tsx:61,81 | — | — |
| 20-3(F-20). `frontend/src/lib/image.ts`는 영수증용이 아님 — **user_recipe 이미지**를 리사이즈해 data URI로 PG `user_recipe.image_url`(text)에 저장하는 유틸("별도 오브젝트 스토리지 없이") | 확인됨 | frontend/src/lib/image.ts:1-4 | — | "image.ts=영수증 업로드 경로" |
| 19-1(F-19). 프롬프트 템플릿 위치 = **코드 하드코딩 상수** — ocr `_SYSTEM`(vision.py), video `_SCHEMA_PROMPT`(extract.py). 설정/DB 아님 | 확인됨 | services/ocr/app/pipeline/backend/vision.py:25-41 · ml/video-recipe/extract.py:17-49 | — | — |
| R-1. 재시도·백오프·타임아웃(ocr) = `asyncio.wait_for` 60s(`gemini_timeout_s`) + transient(문자열 매칭 503/429/500/overloaded 등) 최대 3시도·`sleep(1.5*attempt)` 선형 백오프 + thinking 거부(400) 시 thinking 제거 1회 폴백 | 확인됨 | services/ocr/app/pipeline/backend/vision.py:72-78,187-213 · app/config.py:48 | — | — |
| R-2. 재시도·백오프(video) = **호출 단위 래퍼 없음**(gemini_extract는 동기 1회 호출) — 잡 전체 `wait_for` 120s + 하드실패 1회 상위모델 재분석이 전부. 백오프 없음 | 확인됨 | ml/video-recipe/extract.py:97-107 · services/video/app/main.py:108-117 | — | — |
| A-1. 아티팩트 로딩 = ocr: Gemini 클라이언트 lifespan 1회, 분류기(gazetteer·shelf_life, DB 우선·단기 커넥션 후 close)는 첫 요청 lazy 싱글턴 · video: gazetteer lifespan 1회 로드 | 확인됨 | services/ocr/app/main.py:35 · app/pipeline/classify.py:171-199,344-351 · services/video/app/main.py:46-55 | — | — |
| RT-1. 런타임 = 양 서비스 `python:3.12-slim`·비루트 uid 10001·FastAPI+uvicorn·google-genai·redis·psycopg3[binary]·prometheus-fastapi-instrumentator(+ocr Pillow). 포트 env `PORT`(기본 8010/8011) | 확인됨 | services/ocr/Dockerfile:4,17 · services/video/Dockerfile:5,19 · services/ocr/requirements.txt:1-10 · services/video/requirements.txt:2-9 | — | — |
| RT-2. 로깅 = ocr는 한 줄 **구조화 JSON**(JsonFormatter, `LOG_LEVEL`/`ENVIRONMENT` env, uvicorn.access 비활성) · video는 표준 `logging`(비구조화, observability 모듈 부재) | 확인됨 | services/ocr/app/observability.py:58-105 · app/main.py:27 · services/video/app/main.py:52-55(관련 formatter 없음) | — | — |
| D-1. ocr 의존 = Redis(잡 상태 외부화 #296, TTL 1h, 장애 시 인메모리 폴백) + PG(gazetteer·shelf_life_ref **읽기 전용 1회**) · Kafka **없음**(참조 0건) | 확인됨 | services/ocr/app/store.py:26-85 · app/config.py:15-17 · app/pipeline/classify.py:171-236 · grep kafka 0건 | — | — |
| D-2. video 의존 = Redis(잡 상태 1h + 교차유저 캐시 30일 + 단일비행 락 — 멱등 키가 아니라 중복 비용 방지) + PG(기동 시 gazetteer 정규화 + 요청 시 재료비 가격 조회) · Kafka **없음** | 확인됨 | services/video/app/store.py:18-20 · app/normalize.py:26-35 · app/cost.py:76-84 · grep kafka 0건 | — | — |
| C-1. 배포(compose) = deploy/app compose에서 ocr·video는 **profile 게이트**(기본 스택 제외, `--profile ocr/video`로만 기동)·Harbor `192.168.0.10` 이미지·SA 키는 호스트 파일 ro 마운트 배선(기본 /dev/null). 루트 docker-compose.yml엔 ocr/video 서비스 없음 | 확인됨 | deploy/app/docker-compose.yml:212-260(ocr)·252-278(video)·240-244(SA 키 volumes) · docker-compose.yml:129-134 | — | — |

## ② as-built 요약

1(영수증 인식)=[확인됨: HTTP 202+폴링, 인프로세스 asyncio 태스크 — 이벤트/큐 아님·**불일치**(기존 "이벤트" 주장)] · 9(품목 분류)=[확인됨: 한 호출 + 로컬 캐스케이드, LLM 티어 미구현·**불일치**(기존 "Gemini 분류")] · 2(영상→레시피)=[확인됨: 202 비동기·BackgroundTasks·120s 상한·ml/video-recipe=동일 프로세스 라이브러리] · 10(구조화 추출)=[확인됨: 단일 Gemini 호출에 내장·**불일치**(Nova 반증)] · 5(LLM 인증)=[확인됨: 기본=개인 Gemini API 키, Vertex는 env 토글 존재 — K8s 라이브 실값은 **미확인**(5-4)·config_canary=드리프트 카나리] · 6(모델 ID)=[확인됨: gemini-3.5-flash-lite ×4 + gemini-3.5-flash ×3, 리전 하드코딩 없음] · F-13=[확인됨: null+needs_review 저장·HITL 표시 / 합계 정합 검증은 ocr 내 미구현, total_anchor는 pantry 계약에만 시사] · F-20=[확인됨: MinIO/S3 아님 — 원본 미저장, 프론트→서비스 multipart 직접] · F-19=[확인됨: 프롬프트=코드 하드코딩] · 재시도=[확인됨: ocr 60s+3회 백오프 / video 호출단위 없음·120s 잡 타임아웃] · 아티팩트 로딩=[확인됨: 기동 1회/첫 요청 lazy] · 런타임=[확인됨: python3.12·FastAPI / 로깅 ocr=JSON·video=비구조화] · Redis/Kafka/PG=[확인됨: Redis=잡 상태·캐시·락, PG=읽기 전용 참조, Kafka=양 서비스 미사용]
