# 06_열린질문.md

> 이 문서가 대체하거나 보완하는 기존 문서: 없음 (신규 — 열린 질문·즉시 신청·콘솔 확인 요청 대장)

## 0. 다음 세션 재개 지점 (2026-08-07 인계 직전 갱신 — 구판 A단계 기록은 §0-구판으로 격하)

### 0-1. 완료한 것

1. **A단계** 실사 전부(FACTS/02_as_built — 불일치 19·NEW 12·미구현 6) + 즉시 신청 목록.
2. **B단계(부분)**: PF 판정 8건 완료(사실 3·부분 5 — `00_진단_판정표.md` §1, 원장 agent-F·agent-PF-resume) · **6건 보류**(PF-07·08·09·10·11·13 — 컨펌 후 재개).
3. **C단계**: 이전 대상 재정의+7R(`01_이전대상_7R.md`, 1단계 정정판 = **이전 6종**) · 축①~⑥(`02_레지던시_네트워크_VPC.md`·`03_실행환경_배선_IAM_비가역.md`).
4. **인계 산출물**: 팀장 검토본 `00_설계안_팀장검토용.md`(결정 요청 D-1~D-6) · 구현 계약 `10_구현_인계_계약.md`(함수 8개 계약표·레포 규약·첫 PR 순서·금지 목록). STATE.md 정정 3건 반영 완료.

### 0-2. 결정 대기 (팀장 — 답이 오기 전 착수 금지 항목 명시)

| 결정 | 기한 | 이 답이 오면 다시 해야 하는 것 |
|---|---|---|
| **D-1 레지던시 (A/B/C)** | 8/9 | **A**: 발표 문구 정정 착수(D-4 와 병합) — 계약표 그대로 진행. **B**: 재실측 계획 수립(8/11 착수 한계)·기준선 4,577원 재산정·계약표 BEDROCK_MODEL_ID/IAM 블록을 서울 단일 ARN 으로 교체. **C**: 계약표의 ocr·video 4개 함수를 Cloud Run 계약으로 재작성 + GCP 가용성·크레딧 확인(콘솔 요청 #8)이 그보다 선행 |
| **D-2 범위 (챗봇 잔류 + NER·가격 배치 이전)** | 8/9 | 승인: 그대로. 거부(3종 전부 잔류로 회귀): `01` 범위표·계약표에서 ner-backfill·price-detect 행 제거, 일정 계수 18~32 → 14~26 복원. 랭킹 서빙 정지 답 → config 레포 PR 로 집행 |
| **D-3 VPC 밖 배치 (조건부)** | 8/9 + **검증 8/10 착수 전** | 승인 + Tunnel 검증(PG·Kafka TCP) 성공: SAM VPC 파라미터 빈 채 유지. 검증 실패 또는 거부: 대안 재결정(NAT 부활 시 검토본 §5 비용표 갱신 + 가격 탐지는 구판 결론(전 구간 잔존)으로 자동 복귀 — `03` 4-4 반증 조건) |
| **D-4 발표 논거 3건 정정** | 문구 8/15 | 승인: 발표자료·예상질문 #27·#16·#13 수정안 작성(리허설 전 반영). 이 작업은 D-1 답과 문구가 연동됨 |
| **D-5 IAM ReadOnlyAccess** | 8/8 | 부여 즉시: CLI 일괄 확인(서울 실단가 4종·Lambda/Bedrock 적용 쿼터·크레딧 잔액) → 검토본 §5·§7 을 실값으로 갱신, `02` 문서의 [리전 미확인·하한] 표기 해제 |
| **D-6 EKS 선후 (가/나/다)** | 8/9 | **'가'(EKS 선행)**: D-3 폐기 + `02` 축②·검토본 네트워크 절 재작성(VPC 내 DB 전제) + 계약표의 경로 셀(PG_DSN·Kafka·Tunnel) 재작성 + 첫 PR 순서 재편(`10` §5 1번 조항 발동) — 단 배치 함수 코드 자체는 재사용 가능하므로 골격 PR(3번)은 유효. **'나/다'**: 현 설계 유지, `10` §5 2번(Tunnel 검증)으로 진행 |

### 0-3. 결정 후에 해야 할 것 (순서 고정 — `10_구현_인계_계약.md` §5)

① D-6 확인 → ② D-3 검증(Tunnel PG+Kafka TCP) → ③ 골격 PR(serverless/ 트리·CRF 아티팩트 확보·런타임 고정 — **결정 3건과 무관, 즉시 가능**) → ④ 요약·감정 배치 2종 → ⑤ NER·가격 배치 2종. 동기 2종(ocr·video)은 D-1 확정 후 별도 열차. 보류 PF 6건 재개는 컨펌 후(진단서 §2-1 연동 카드가 CS·OP·PR 판정의 승계 원점).

### 0-구판 (A단계 종료 시점 기록 — 이력 보존용)

- 미독 자료: AI실측기록_html 7종 · ai-model-selection-final.md · schema-public-data.sql(리뷰 DDL) · mealplanning-config NetworkPolicy 상세. 미답 [Q]: Q-01(계정 소유 구조). 잔여 [미확인]: FACTS/02_as_built §4 의 5건 + 검토본 §7 의 12건(정본은 검토본 쪽).

## 1. 즉시 신청 목록 (§3.5 — 잠정 v1, 2026-08-07 출력. 이후 Z-3에서 정밀화하되 이 목록은 취소하지 않는다)

**전제: 리드타임이 [미확인]인 항목일수록 오늘(8/7) 신청이 안전하다 — 리드타임을 모른다는 것 자체가 오늘 내야 할 이유다.**

| # | 항목 | 신청 화면 경로 | 예상 리드타임 | 이것이 없으면 8/13에 무엇이 불가능해지는가 |
|---|---|---|---|---|
| 1 | **AWS 계정·결제수단·크레딧($700) 적용 상태 확인** (모든 신청의 전제) | AWS 콘솔 → Billing and Cost Management → Credits / Payment preferences | 계정이 이미 있으면 0일. 계정 소유 구조 자체가 [미확인] | 계정·결제수단 없이는 아래 전 항목 신청 불가 → 8/13 실측 전면 불가 |
| 2 | **Bedrock Anthropic 최초 사용(FTU) use case 폼 제출** (계정당 1회) | Bedrock 콘솔 → Bedrock configurations → Model access → Modify model access → Anthropic 모델 선택 → Submit use case details (또는 `PutUseCaseForModelAccess` API). 학생은 회사 웹사이트 대신 GitHub/포트폴리오 URL 허용 | **제출 즉시 액세스 부여** [출처: docs.aws.amazon.com/bedrock/latest/userguide/model-access.html, 조회 2026-08-07] | 미제출 시 Claude 계열 첫 호출이 `AccessDeniedException` → 리뷰 요약(sonnet-3.5 표기 모델) 배치 실측 불가 |
| 3 | **Bedrock 모델 자동 구독 사전 완료** (nova-micro·nova·claude 각각 1회 호출 또는 콘솔 활성화) | 위와 동일 화면. Marketplace 권한(`aws-marketplace:Subscribe`·`Unsubscribe`·`ViewSubscriptions`) 선행 필요 | 첫 호출 시 백그라운드 구독 **최대 15분** [출처: 동일 URL, 조회 2026-08-07] | 8/13 당일 첫 호출이면 AccessDenied 간헐 발생으로 측정 시작 지연. ⚠️ 서울(ap-northeast-2) 제공 여부는 별도 문제 [미확인 — B-03, B단계 판정] |
| 4 | **Lambda 리전당 계정 동시 실행 한도 상향** | Service Quotas 콘솔 → AWS services → AWS Lambda → Concurrent executions → Request increase at account level | [미확인] — 신규·저이력 계정은 기본값이 표준값보다 낮을 수 있음(현재값도 [미확인] → 콘솔 확인 요청 #2) | 한도가 낮으면 부하 테스트 스로틀 + 프로비저닝 동시성 설정 자체가 한도를 잠식 → PoC 측정 왜곡 |
| 5 | **Bedrock 모델별 TPM·RPM 쿼터 상향** | Service Quotas 콘솔 → AWS services → Amazon Bedrock → (모델별) InvokeModel requests/tokens per minute → Request increase | [미확인] — 조정 가능 여부가 쿼터 항목별로 다름 | 리뷰 요약 6,873건 배치가 429 연발 → 재시도로 크레딧 소모 + 완주 실측 불가 |
| 6 | **Vertex/Gemini 쿼터 확인·상향** (⚠️ 실사 F-16·인증 방식 확정과 연동 — 개인 Gemini API 키 운영 중이라는 기록 존재) | (a) Vertex AI 경로: GCP 콘솔 → IAM & Admin → Quotas → `aiplatform.googleapis.com` 필터 (b) 개인 Gemini API 키 경로: aistudio.google.com → 요금제·rate limit 확인 | [미확인] | 영수증 인식·영상→레시피 추출 실측이 429로 중단. 어느 경로(a/b)인지 자체가 실사 대상 |
| 7 | **(조건부) Site-to-Site VPN 또는 DX 회선** — H축(하이브리드 네트워크) 판정 전이지만 리드타임이 가장 긴 후보 | VPC 콘솔 → Site-to-Site VPN connections → Create VPN connection. 온프렘 측 공인 IP·IPsec 지원 라우터 사양 조사는 **오늘 시작 가능** | VPN 리소스 생성 자체는 시간 단위, 온프렘 라우터 설정 포함 총 소요 [미확인]. DX는 회선 개통이 주 단위로 알려져 있어 8/18 내 불가 개연성 — 정확 값 [미확인] | Lambda→온프렘 PG 경로가 필요한 설계로 판정될 경우, 회선이 없으면 8/9 이후 배선 작업 전체 착수 불가 |
| 8 | **(로컬 전제) aws CLI v2 + 자격증명 구성, gcloud CLI** | 로컬 설치 (신청 아님) | 설치 즉시 | 이 세션에서 이미 발생: CLI 부재로 쿼터·모델 목록의 기계 근거 수집 불가 → 콘솔 수동 확인에 의존 |

### 1-1. v1.1 갱신 (실사 반영, 2026-08-07 — v1 목록은 취소하지 않음)

1. **신청 대상 모델의 실물 ID 확정** (#2·#3·#5에 적용): `apac.anthropic.claude-3-5-sonnet-20241022-v2:0` · `apac.amazon.nova-micro-v1:0` [FACTS/02 D-19]. 둘 다 **크로스리전 추론 프로파일 ID** — FTU 폼·쿼터 상향 신청 시 이 ID 기준으로 신청하되, 서울 단독 호출 가능 여부(B-03)가 `거짓`으로 판정되면 모델 교체·리전 협상이 신청보다 먼저다.
2. **#6은 경로 (a) Vertex로 확정**: 라이브 = Vertex, GCP 프로젝트 `mealplanning-503911` [FACTS/02 D-02] → GCP 콘솔 → IAM & Admin → Quotas → `aiplatform.googleapis.com`에서 해당 프로젝트 쿼터 확인·상향.
3. **#7 온프렘 라우터 조사에 추가**: 클러스터에 이미 Cloudflare Tunnel(아웃바운드) 유입 경로가 가동 중 [NEW-01] — Lambda→온프렘 경로 검토 시 VPN/DX 외에 기존 터널·리버스 프록시 계열이 실물 후보로 존재함을 H축 비교에 반영.

## 2. 미확보·위치 특이 파일 (§3-2)

1. `09_프롬프트_설계근거.md` — 레포에 없음. **레포 밖 `/mnt/d/dz_bravo/serverless-migration/09_프롬프트_설계근거.md`(316,616B)로 접근 가능** — 미확보 아님. 전문 읽기 금지(§3-3), 목차 인덱싱만.

## 3. [Q] 사람에게 질문 (상한 12 — B단계에서 채움)

| # | 질문 | 보수적 임시 가정 | 답이 다르면 바뀌는 것 |
|---|---|---|---|
| Q-01 | AWS/GCP 계정 소유 구조 — 계정 생성 주체, 루트 보관자, **결제 수단이 개인 카드인가**, $700 크레딧이 묶인 계정 | 개인 카드 연결 단일 계정 (§6-FF-3 보수 기본값) | 서킷브레이커 임계·환경 분리(Z-1)·즉시 신청 목록 #1 |

## 4. 콘솔 확인 요청 목록 ([Q] 상한에 미포함 — aws CLI 부재로 필수)

| # | 확인할 값 | 콘솔 경로 또는 CLI 명령 | 왜 필요한가 | 없으면 어느 산출물이 빈칸인가 | 회신 마감 |
|---|---|---|---|---|---|
| 1 | Bedrock 모델별 액세스 활성화 상태 (nova-micro·nova·claude 계열) | Bedrock 콘솔 → Model access (또는 `aws bedrock get-foundation-model-availability --model-id <id>`) | 액세스 없으면 8/13 실측 시작 불가 | 03d 쿼터표·즉시 신청 목록 #2·#3 판정 | 8/8 |
| 2 | Lambda 동시 실행 한도 현재값 | Service Quotas → AWS Lambda → Concurrent executions (또는 `aws service-quotas get-service-quota --service-code lambda --quota-code L-B99A9384`) | 신규 계정 기본값 확인 → 상향 필요량 산정 | A 한도표·Z-1 폭발 반경 산술 | 8/8 |
| 3 | Bedrock 모델별 TPM·RPM 현재 쿼터 | Service Quotas → Amazon Bedrock → 모델별 항목 | 배치·챗봇 동시 수용량 판정 | Z-2 쿼터 공유 산술 | 8/8 |
| 4 | 크레딧 잔액·만료일 | Billing → Credits | $700 잔액이 실험 예산표(X-3)의 상한 | X-3 실험 예산표 | 8/8 |
| 5 | 계정 생성일 | Billing → Account (또는 `aws sts get-caller-identity` 후 support 문의) | 저이력 계정 여부 → 초기 한도 추정 | Z-3 리드타임 역산 | 8/9 |
| 6 | Vertex 프로젝트 ID·쿼터 (또는 개인 Gemini API 키의 요금제) | GCP 콘솔 → IAM & Admin → Quotas / aistudio.google.com | 인증 방식 실사 결과와 대조 | 03b 인증 축·즉시 신청 #6 | 8/9 |
| 7 | **ap-northeast-2 실단가 4종**: NAT GW $/h·$/GB, 인터페이스 엔드포인트 $/h/AZ, Bedrock `claude-3-5-sonnet-20240620`·`claude-3-haiku`·nova-micro 1M 토큰 단가 | AWS Pricing Calculator(calculator.aws) 또는 각 요금 페이지에서 리전=Seoul 선택 | 축①②의 모든 단가가 [리전 미확인 · 기본값 추정](웹캐시 예시 리전 값) — 서울 실단가로 배수 하한을 확정치로 승격 + B안 비용 칸 채움 | `02_레지던시_네트워크_VPC.md` 2-1 표·1-2 B안 비용 | 8/8 |
| 8 | **GCP asia-northeast3 가용성 3종 + 크레딧**: Cloud Run·Eventarc 제공 여부, Vertex Gemini(flash-lite·flash) 서울 호출 가능 여부, 프로젝트 `mealplanning-503911` 결제·크레딧 잔액 | GCP 콘솔 → 리전 선택 화면 / Vertex AI → Model Garden 리전 필터 / Billing (또는 `gcloud run regions list`·`gcloud ai models list --region asia-northeast3` — 11_실측발견 §7 명령) | C안(GCP 서버리스)의 성립 전제 — 하나라도 불가면 C 는 비교표에서 제거 | `02_레지던시_네트워크_VPC.md` 1-2 C안·2-3 대안 2 | 8/8 |
