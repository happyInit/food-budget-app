# 00_요약판정.md

> 이 문서가 대체하거나 보완하는 기존 문서: 계획 문서 11종(`docs/serverless-migration/00~08·README·REVIEW_BRIEF`)에 대한 판정 요약 — 신규, 어느 것도 덮어쓰지 않음.

## 0. 세션 상태 (2026-08-07, A단계 진행 중)

1. 이번 세션 범위 = A단계(근거원·문서·레포 지도 + as-built 실사 + 즉시 신청 목록)뿐이다. **유지/변경/폐기 판정과 "가장 위험한 5가지"는 B·C단계 산출이며 이 파일에는 아직 뼈대만 있다.**
2. A단계에서 확정된 사실은 `STATE.md`·`FACTS/`에, 즉시 행동 항목은 `06_열린질문.md` §1에 있다.

## 1. 가장 위험한 발견 5가지 (미작성 — B·C단계)

| # | 발견 | 근거 | 일정·비용·서사 영향 |
|---|---|---|---|
| — | (A단계 종료 — §3의 잠정 후보를 B단계 판정으로 승격 예정) | | |

## 2. 유지/변경/폐기 결정 (미작성 — C단계)

## 3. A단계 잠정 관찰 (판정 아님 — 근거 전부 `FACTS/02_as_built.md`)

1. **이전 대상 "10종"의 실체가 문서와 다르다**: #9·#10은 독립 기능이 아니라 #1·#2에 내장된 단계(D-04·D-05), #7 NER은 동기 저지연이 아니라 배치 백필 전용(D-06), #5는 배선만 있고 flag OFF(D-18), #3 챗봇은 라이브에서 LLM을 아예 안 부른다(template 모드, D-07). → "최대 난관=NER 동기 저지연" 전제와 8/7 PoC 범위·임계 경로가 흔들린다 (판정은 B단계).
2. **S3 이벤트 트리거 전제 무효**: 영수증 원본은 어디에도 저장되지 않는다(D-11) — 업로드 경로 신설 없이는 이벤트 기반 서버리스 설계의 대상 자체가 없다.
3. **Bedrock 실물 모델 ID 2종이 전부 `apac.` 크로스리전 추론 프로파일**(D-19) — "양쪽 서울 리전" 원칙과의 충돌 여부가 B-03 판정의 직접 입력이 된다.
4. **Kafka·자동 CD가 이미 실가동**(D-09·D-10) — "Kafka=Phase 2 밖"·"수동 반영" 전제로 짠 병행 운영·이중 실행 차단 설계는 실물 기준 재작성 필요.
5. **TZ 혼재**(D-17): CronJob=KST, 컨테이너 프로세스=UTC, 날짜 로직은 KST 명시/프로세스 로컬/DB 세션 의존이 뒤섞임 — "100% 완전 일치" 검증 기준의 성립 조건에 직결.
6. **PGSync는 지금 죽어 있다**(D-13, 슬롯 invalidated 크래시루프) — "상시 연결" 전제의 반증이자 라이브 인시던트.
7. 파이프라인은 pooler 미경유 pg-rw 직결 + 정적 AWS 키 + 관측 print뿐(§1 표) — 서버리스 이전과 무관하게 현행 약점 3종.
