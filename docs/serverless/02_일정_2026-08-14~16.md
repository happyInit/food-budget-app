# 일정 — AI 서버리스 이전 (2026-08-14 ~ 08-16)

> **목표** 8/15 자정 = AI 12함수 동작 확인 · 8/16 자정 = 전체 정상동작 + 픽스 + 문서
> **오늘 기준 실측** 5/12 완료(배치) · 7/12 미착수 · **AWS 배포 권한 미도착**

---

## 🔴 먼저 — 목표를 두 층으로 나눠야 성립합니다

*"동작 확인"* 을 한 단어로 두면 **남의 손에 달린 것과 내 손에 있는 것이 섞입니다.**

| | 게이트 | 무엇을 증명하나 | 막는 것 |
|---|---|---|---|
| **L** | **로컬 확인** | arm64 Lambda 런타임에서 핸들러가 실제로 호출되고 의도한 자리에서만 실패 | 🟢 **없음** — 오늘도 5종을 이렇게 검증했다 |
| **A** | **AWS 확인** | 함수가 배포되어 큐·스케줄·모델·DB 와 실제로 붙는다 | 🔴 **권한**(미도착) · 🔴 **내부 NLB**(인프라 작업) |

**8/15 자정 = L 게이트 12/12** 를 목표로 잡습니다. **A 게이트는 권한·NLB 가 오는 시점에 따릅니다** — 오면 같은 날, 늦으면 8/16.

🔴 **A 를 8/15 목표로 잡으면 지킬 수 없는 약속이 됩니다.** 지금 `lambda:ListFunctions` 조차 거부되고, PG·ES 는 `172.20/16`(EKS 서비스 대역)이라 라우팅 자체가 없습니다.

---

## 8/14 (오늘 남은 시간) — **접수·워커 계약을 세운다**

가장 어려운 것을 먼저 한다. 나머지 3종은 이 본을 따라간다.

| # | 할 일 | 산출 | 비고 |
|:--:|---|---|---|
| 1 | **공통 계약 모듈** — 잡 상태·단일비행 락·큐 전송을 `serverless/common/jobs.py` 로 | 코드 | `video/store.py` 계약을 그대로 옮긴다(재설계 X) |
| 2 | **`ai-video-api`** 접수 핸들러 | 코드 | 캐시 히트 → 워커 안 부름 · 락 실패 → 409 · SQS 전송 |
| 3 | **`ai-video-worker`** 소비 핸들러 | 코드 | 🔴 중복 방어(진입부 캐시 재확인) + **마지막 시도 `FAILED` 기록** |
| 4 | **로컬 검증** — arm64 런타임에 SQS 이벤트를 직접 주입 | 로그 | 큐 없이도 워커 경로가 검증된다 |
| 5 | 🔴 **G-06 을 팀에 올린다** (사용자) | 메시지 | `ocr` 이미지 전달 경로 — **이게 병렬로 안 돌면 8/15 에 `ocr` 2종이 밀린다** |

**끝나면** 7/12 → 배치 5 + video 2 = **7/12 L 게이트**

---

## 8/15 — **나머지 5종 + 12종 일괄 검증**

| # | 할 일 | 산출 | 위험 |
|:--:|---|---|---|
| 1 | **`ai-notify-consumer`** — SQS 소비. video-worker 와 같은 골격 | 코드 | 🟡 **C-88 로 알림큐가 불요해졌을 수 있다** — 먼저 확인(30분) |
| 2 | **`ai-rank-serve`** — 동기. LightGBM 모델 로드 | 코드 | 🔴 **모델 파일이 없다**(이슈 #649). 없으면 «로드 실패를 올바르게 보고하는가» 까지만 검증 |
| 3 | **`ai-chat-api`** — 동기 FastAPI | 코드 | 🔴 **ASGI 어댑터 필요**(Mangum). 배치와 성격이 다르다 — 여기서 시간이 샐 수 있다 |
| 4 | **`ocr` 2종** — G-06 결정이 오면 | 코드 | 🔴 **미결이면 `C안`(EKS 잔류)으로 잡고 10/12 로 마감** |
| 5 | **12종 일괄 빌드 + 런타임 검증** — 크기·import·핸들러 호출 | 표 | L 게이트 판정 |

**끝나면 L 게이트 12/12** (또는 G-06 미결 시 10/12)
**권한이 도착해 있으면** 같은 날 저녁에 배포 착수 → A 게이트 시작

---

## 8/16 — **연결·픽스·문서**

| # | 할 일 | 산출 | 전제 |
|:--:|---|---|---|
| 1 | **AWS 배포** — 12종 · 큐 2 + DLQ 2 · 스케줄 3 | 실물 | 🔴 권한 |
| 2 | **데이터 연동 검증** — PG·ES·Valkey 실접속 | 로그 | 🔴 **내부 NLB** |
| 3 | **실패 경로 검증** — DLQ 적재 · 자진 중단 · 알람 | 로그 | 🔴 `cloudwatch:PutMetricAlarm`(요청함) |
| 4 | **픽스** — 1~3 에서 나온 것 | 커밋 | |
| 5 | **문서 정리** — 설계·결정·잔여 결함 대장 갱신 | 문서 | 🟢 막는 것 없음 |

🔴 **1·2 가 안 오면 3·4 가 통째로 밀립니다.** 그때는 **5(문서)를 앞당기고** L 게이트 결과로 마감합니다.

---

## 지금 남은 결함 중 이 일정에 걸리는 것

| | 상태 | 영향 |
|---|---|---|
| **G-06** 이미지 전달 | 🔴 미결 | `ocr` 2종 (8/15 #4) |
| **G-01** 모델 백업 | 🔴 미결 | `.crfsuite` 308KB 를 S3 로 — **버킷 이름이 `mp-ai-*` 여야 접근된다** |
| **V-13** 알람 결측 | 🔴 권한 대기 | 8/16 #3 |
| 내부 NLB | 🔴 인프라 | 8/16 #2 |
| 권한 | 🔴 미도착 | 8/16 #1 |

---

## 정직한 위험

**8/15 L 게이트 12/12 는 두 가지에 걸려 있습니다.**
① **G-06 결정**(오늘 올려야 함) ② **`chat-api` ASGI 이식 난이도**(미지수)

**8/16 A 게이트는 제 손 밖입니다.** 권한과 NLB 가 오면 하루면 되고, 안 오면 **L 게이트 + 문서**로 마감합니다.
→ **그 경우에도 "무엇이 준비됐고 무엇이 막혔는지" 가 문서로 남습니다.** 그게 8/16 #5 입니다.
