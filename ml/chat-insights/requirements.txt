# 코어는 표준 라이브러리만(리포트·선호 추출). 아래는 선택/배포용.
scikit-learn>=1.3          # 의도 분류기 폴백(FastText 미설치 시)
psycopg[binary]>=3.2,<4    # _data.py 실 chat_message 읽기(스냅샷 1회)
python-dotenv>=1.0
# fasttext>=0.9            # 정식 의도 분류기(선택 — 없으면 sklearn 폴백)
# google-genai>=0.3        # 리포트 서술 분석(선택 — REPORT_GEMINI_API_KEY 있을 때만)
