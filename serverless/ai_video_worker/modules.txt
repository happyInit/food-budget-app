# 이 함수가 실제로 쓰는 레포 모듈만 적는다(build.sh 가 이 목록만 번들에 넣는다)
#
# 워커는 추출 전체를 돈다 — 접수와 달리 `extract.py`(Gemini/Vertex 호출)가 필요하다.
# 🔵 `services/video/app/*` 는 하나도 안 들어간다. 거기는 FastAPI 껍데기라 Lambda 에서 쓸 것이 없다
#    (잡 상태·락·캐시는 `serverless/common/jobs.py` 가 같은 Valkey 키로 대신한다).
# ⚠️ 재료비(`services/video/app/cost.py`)는 **이번 범위 밖**이다 — PG 접속이 붙는 별개 관심사라
#    접수·워커 분할이 검증된 뒤 별건으로 얹는다(app.py 의 `_done_payload` 주석과 짝).
ml/video-recipe/pipeline.py
ml/video-recipe/extract.py
ml/video-recipe/models.py
ml/video-recipe/validate.py
