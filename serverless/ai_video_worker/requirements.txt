# mp-ai-video-worker — 추출 본체. **락 파일이다(전이 의존까지 핀).**
#
# 🔴 전이까지 박는 이유는 api 쪽 requirements.txt 머리말과 같다 — `--python-version` 은
#    휠 선택만 바꾸고 **환경 마커는 빌드 기계 기준**이라, 상위만 적으면 조용히 빠진다.
#
# 🔵 `boto3` 없음 — Lambda 런타임 제공.
# 🔵 `psycopg` 없음 — 재료비(cost.py)를 이번 범위에서 뺐다(app.py `_done_payload` 주석).
#    붙이는 순간 PG 접속·VPC·롤이 함께 붙으므로 분할 검증이 끝난 뒤 별건으로 얹는다.
#
# 갱신법: pip download --only-binary=:all: --platform manylinux2014_aarch64 \
#           --python-version 3.12 -d /tmp/w 'pydantic>=2.9,<3' 'redis>=5.0,<6' 'google-genai>=1.0,<2'
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
cryptography==50.0.0
distro==1.9.0
google-auth==2.56.3
google-genai==1.75.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
pyasn1==0.6.4
pyasn1-modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic-core==2.46.4
pyjwt==2.13.0
redis==5.3.1
requests==2.34.2
sniffio==1.3.1
tenacity==9.1.4
typing-extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
websockets==16.1.1
