# 이 함수가 실제로 쓰는 레포 모듈만 적는다(build.sh 가 이 목록만 번들에 넣는다)
#
# 🔵 **비어 있다. 일부러 그렇다.**
#    접수는 파싱을 하지 않는다 — 이미지를 S3 에 두고 큐에 넣고 202 를 돌려줄 뿐이다.
#    `services/ocr/app` 을 담으면 `google-genai`·Pillow·psycopg 가 통째로 딸려와
#    **타임아웃 10초 함수의 콜드스타트**를 무겁게 만든다. 무거운 것은 전부 워커 몫이다.
#    (`video-api` 가 `extract.py` 를 일부러 뺀 것과 같은 판단이다.)
#
# ⚠️ 응답 모델(`app/models.py`)도 안 쓴다 — 접수 응답은 `{job_id, status}` 두 필드라
#    pydantic 을 끌어올 이유가 없다. 폴링 응답은 **워커가 넣은 JSON 을 그대로 돌려준다**
#    (모양의 정본은 워커 쪽이고, 여기서 다시 검증하면 두 벌이 갈린다).
